{
  "name": "write_file",
  "description": "Write contents to a file in app-private storage",
  "parameters": {
    "properties": {
      "path": {
        "type": "string",
        "description": "File path relative to app storage (e.g., 'documents/output.txt') or an absolute path within app storage"
      },
      "content": {
        "type": "string",
        "description": "The content to write to the file"
      },
      "mode": {
        "type": "string",
        "description": "Write mode: 'overwrite' (replace file) or 'append' (add to end). Defaults to 'overwrite'.",
        "enum": ["overwrite", "append"],
        "default": "overwrite"
      }
    },
    "required": ["path", "content"]
  },
  "requiredPermissions": [],
  "timeoutSeconds": 10
}
