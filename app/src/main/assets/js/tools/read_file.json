{
  "name": "read_file",
  "description": "Read the contents of a file from app-private storage",
  "parameters": {
    "properties": {
      "path": {
        "type": "string",
        "description": "File path relative to app storage (e.g., 'documents/notes.txt') or an absolute path within app storage"
      },
      "encoding": {
        "type": "string",
        "description": "File encoding. Defaults to 'UTF-8'.",
        "default": "UTF-8"
      }
    },
    "required": ["path"]
  },
  "requiredPermissions": [],
  "timeoutSeconds": 10
}
