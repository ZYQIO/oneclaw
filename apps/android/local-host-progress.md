# Android Local Host Progress / Android 本机 Host 进度

Scope / 范围: track the Android effort to run OpenClaw locally on the phone, authenticate with Codex, and expose a safe remote-control surface. / 追踪 Android 端在手机本机运行 OpenClaw、使用 Codex 授权，并暴露安全远程控制面的推进情况。

## Objective / 目标

Ship an Android-hosted MVP that can: / 交付一个运行在 Android 手机上的 MVP，具备以下能力：

- 在手机上以 `Local Host` 模式本地运行 OpenClaw。Run OpenClaw locally on the phone in `Local Host` mode.
- 使用 `openai-codex` 完成授权。Authenticate with `openai-codex`.
- 通过 GPT 完成本地聊天的发送与返回。Send and receive local chat turns through GPT.
- 为可信远端客户端提供带保护的远程控制 API。Expose a guarded remote-control API for trusted remote clients.

## Definition Of Done / 完成定义

This effort is considered done when all of the following are true. / 当且仅当以下条件全部满足时，才视为该任务完成。

- Android can start `Local Host` without relying on an external Gateway. / Android 可以在不依赖外部 Gateway 的情况下启动 `Local Host`。
- Codex sign-in works from the app and survives token refresh. / Codex 登录可在 App 内完成，并且 token 刷新后仍然可用。
- Local-host chat succeeds end-to-end on a real phone. / 本机 Host 聊天链路已在真实手机上端到端跑通。
- A trusted remote client can call `/status`, `/chat/send-wait`, and at least one `/invoke` command successfully. / 可信远端客户端可以成功调用 `/status`、`/chat/send-wait` 以及至少一个 `/invoke` 命令。
- Remote command tiers are explicitly gated and visible in UI and API. / 远程命令分层具备明确开关，并在 UI 和 API 中可见。
- Real-device validation evidence exists for the happy path and key failure paths. / 已有真实设备上的成功路径和关键失败路径验证证据。
- Android build, unit tests, and smoke validation pass in a reproducible environment. / Android 构建、单测和冒烟验证可在可复现环境中通过。

## Non Goals / 非目标

The current MVP does not aim to deliver all desktop Gateway features. / 当前 MVP 不以覆盖桌面 Gateway 的全部能力为目标。

- 不追求与 Node Gateway 的完整插件运行时对齐。No full plugin runtime parity with the Node Gateway.
- 不追求与 Web Control UI 的完整控制台对齐。No full control UI parity with the web Control UI.
- 不提供不受限制的远程执行或 shell 访问。No unrestricted remote execution or shell access.
- 不默认做未加固的公网暴露。No public internet exposure without explicit network hardening.

## Milestones / 里程碑

### M1. Local Host Foundation / 本机 Host 基础层

- [x] 在 Android UI 中加入 `Local Host` 运行模式。Add `Local Host` runtime mode in Android UI.
- [x] 持久化本机 Host 设置和 Codex 凭证状态。Persist local-host settings and Codex credential state.
- [x] 将聊天和语音代码改为通过抽象传输层复用本地与远端后端。Route chat and voice code through transport abstractions so local and remote backends can share UI.
- [x] 搭建本机 Host 运行时，并支持 session、history、chat 方法。Stand up a local-host runtime with session, history, and chat methods.

### M2. Codex Auth And Model Calls / Codex 授权与模型调用

- [x] 加入原生 Codex OAuth 登录流程。Add native Codex OAuth login flow.
- [x] 持久化 Codex 凭证并支持刷新。Persist Codex credential and support refresh.
- [x] 在本机 Host 聊天中接入 Codex Responses。Call Codex Responses from local-host chat.
- [x] 在真实 Android 设备上验证完整登录与刷新流程。Verify the complete login and refresh flow on a real Android device.

### M3. Remote Access MVP / 远程访问 MVP

- [x] 加入带 bearer token 保护的本机 Host 远程 API。Add bearer-token protected local-host remote API.
- [x] 加入聊天路由、事件轮询和同步等待聊天结果。Add chat routes, event polling, and sync chat wait.
- [x] 加入自描述路由：`/status`、`/examples`、`/invoke/capabilities`。Add self-describing routes: `/status`, `/examples`, `/invoke/capabilities`.
- [x] 将远程命令拆分为只读、相机高级、写操作三级。Split remote commands into read, camera-advanced, and write-capable tiers.
- [x] 验证真实远端客户端可以经网络驱动手机。Verify a real remote client can drive the phone over the network.
- [x] 在可信桌面连接场景下补上 Codex 授权同步路径。Add a trusted-desktop Codex auth sync path for connected-phone recovery.
- [x] 把桌面侧补授权扩成可持续 watch 守护模式。Extend desktop-side auth refill into a continuous watch mode.

### M4. Validation And Hardening / 验证与加固

- [x] 提供可复用的本机 Host 远控冒烟脚本。Add a reusable local-host remote smoke script.
- [x] 在装有 Java 的环境中跑通 Android Gradle 编译和单测。Run Android Gradle compile and unit tests in an environment with Java installed.
- [x] 安装到真实 Android 手机并验证主成功路径。Install on a real Android phone and validate the happy path.
- [x] 记录 Codex 授权失效 / 缺失、权限缺失、远程命令层关闭等失败行为。Capture failure behavior for Codex auth loss/missing auth, missing permissions, and disabled remote tiers.
- [x] 复核远程访问默认值、token 轮换流程和网络暴露指引。Review remote access defaults, token rotation flow, and network guidance.
- [x] 判断 MVP 是否还需要开放更多远程命令。Decide whether any additional remote commands should be enabled for MVP.

## Current State / 当前状态

Completed implementation highlights / 已完成实现要点:

- `Local Host` 模式已经出现在 Connect tab，并作为一等运行模式暴露。`Local Host` mode exists in the Connect tab and is exposed as a first-class runtime option.
- Codex OAuth 登录已经具备浏览器流程和手动粘贴回退。Codex OAuth login exists with browser flow and manual paste fallback.
- 本机远控接口现在除 `auth/codex/status` 和 `auth/codex/refresh` 之外，还支持受保护的 `auth/codex/import`，可信桌面可把本机 `openai-codex` OAuth 凭证同步到手机，避免手机端授权失效后频繁重新走浏览器。The local-host remote API now supports a guarded `auth/codex/import` route in addition to `auth/codex/status` and `auth/codex/refresh`, allowing a trusted desktop to sync its `openai-codex` OAuth credential down to the phone so phone-side auth loss does not force repeated browser logins.
- 桌面侧 `pnpm android:local-host:codex-sync` 现在还支持 `--wait-for-device`，并且 watch 模式会把短暂 adb / 本地连接失败当作可恢复事件继续守护；这使它更接近“电脑检测到手机连接后持续帮手机保住 Codex 授权”，而不是一条容易因断线而中止的命令。Desktop-side `pnpm android:local-host:codex-sync` now also supports `--wait-for-device`, and watch mode now treats short adb / local transport failures as recoverable events instead of exiting; this makes it much closer to “the desktop notices the phone connection and keeps guarding Codex auth for the phone” rather than a fragile command that dies on the first disconnect.
- 现在仓库里还多了一个更薄的默认守护入口 `pnpm android:local-host:codex-guard`，它就是 `--use-adb-forward --wait-for-device --watch` 的 opinionated wrapper；同时 watch 的 JSON 输出也开始显式区分 `kind=lifecycle|iteration|error`，方便外层守护器消费连接状态和同步结果。The repo now also has a thinner default guard entry as `pnpm android:local-host:codex-guard`, which is an opinionated wrapper for `--use-adb-forward --wait-for-device --watch`; watch JSON output also now explicitly distinguishes `kind=lifecycle|iteration|error`, making it easier for an outer supervisor to consume connection state changes separately from sync results.
- guard 现在还支持 `--artifact-dir`，会把 `events.jsonl`、`latest.json` 和 one-shot `summary.json` 持续写到稳定目录里；这让后续 launchd/tmux/agent 包装层可以直接盯文件，而不用再解析终端文本。The guard now also supports `--artifact-dir`, which continuously writes `events.jsonl`, `latest.json`, and one-shot `summary.json` into a stable directory; that lets any later launchd/tmux/agent wrapper watch files directly instead of scraping terminal text.
- 这轮又把桌面 guard 包成了 macOS `launchd` helper：`pnpm android:local-host:codex-guard:launchd` 现在可以安装 / 查看状态 / 卸载一个 per-user LaunchAgent，用外部 env file 提供 bearer token，并把 `adb` 绝对路径和 guard artifacts 一起固定下来。This round also packaged the desktop guard as a macOS `launchd` helper: `pnpm android:local-host:codex-guard:launchd` can now install / inspect / uninstall a per-user LaunchAgent, source the bearer token from an external env file, and pin both the absolute `adb` path and the guard artifacts directory.
- 为了让这条 launchd 守护路径不依赖图形会话里的 PATH，`pnpm android:local-host:codex-sync` 现在还额外支持 `--adb-bin`，可显式指定 `adb` 二进制路径。To keep that launchd guard path independent of the GUI session PATH, `pnpm android:local-host:codex-sync` now also supports `--adb-bin`, letting callers pin an explicit `adb` binary path.
- 现在 `pnpm android:local-host:codex-guard:launchd` 还新增了 `write-env`，可以直接在默认 `~/.openclaw/android-local-host-codex-guard/guard.env` 写入模板或从当前 shell/token 参数写入真实 bearer token；`status` 也会额外回报 env file 是否存在、token 是否仍是占位符。`pnpm android:local-host:codex-guard:launchd` now also has `write-env`, which can write a template straight into the default `~/.openclaw/android-local-host-codex-guard/guard.env` or seed it from the current shell / a `--token` argument; `status` now also reports whether the env file exists and whether the token is still only the placeholder.
- `status` 这轮又多了一层更直接的可执行信号：现在会给出 `recommendedAction=write-env|configure-token|install|check-launchagent|healthy`，所以外层自动化或人工排障不需要自己再拼状态机。`status` gained a more directly actionable signal this round too: it now returns `recommendedAction=write-env|configure-token|install|check-launchagent|healthy`, so outer automation or manual troubleshooting no longer needs to build its own state machine first.
- 在这之上，这轮又新增了 `setup` 子命令，把首次安装和自愈入口进一步收成一个幂等动作：env file 缺失时会写模板，提供 token 时会写入真实值，而 guard ready 之后会自动 install / repair LaunchAgent。On top of that, this round also adds a `setup` subcommand that compresses first-install and self-heal into one idempotent action: when the env file is missing it writes a template, when a token is provided it writes the real value, and once the guard is ready it automatically installs / repairs the LaunchAgent.
- `status` 这轮又多补了一层 `recommendedCommand`：除了动作名，它现在还能直接给出可复制执行的下一条命令，便于人工操作和外层自动化直接复用。`status` gained another layer this round with `recommendedCommand`: beyond the action name, it can now emit the next copy-pastable command directly, which is easier for both human operation and outer automation to reuse.
- 这轮还把几条最常用的 launchd 操作补成了更短的 repo wrapper：`pnpm android:local-host:codex-guard:setup|status|write-env|uninstall`，所以日常使用和 `recommendedCommand` 都不必再暴露底层 `launchd -- <subcommand>` 形式。This round also added shorter repo wrappers for the most common launchd actions: `pnpm android:local-host:codex-guard:setup|status|write-env|uninstall`, so day-to-day use and `recommendedCommand` no longer need to expose the lower-level `launchd -- <subcommand>` form.
- cross-app probe / sweep 现在还支持 `--describe` 离线预览，并把 `followUp.mode`、`followUp.requestedCount` 与 `probeCommand` 写进落盘摘要；因此下一轮 cross-app 跟进不需要先手工拼环境变量或翻旧终端历史。The cross-app probe / sweep now also support an offline `--describe` preview and write `followUp.mode`, `followUp.requestedCount`, and `probeCommand` into their persisted summaries, so the next cross-app follow-up no longer has to start by hand-building env vars or digging through old terminal history.
- 现在还多了一条 debug-only 的 adb token bootstrap：`pnpm android:local-host:token` 会拉起当前 debug app、触发 app 进程内的导出桥，然后直接把当前 local-host bearer token 从 `run-as` 路径带回桌面，因此不需要每次回 Connect 页手抄 token。There is now also a debug-only adb token bootstrap as `pnpm android:local-host:token`: it launches the current debug app, triggers the in-process export bridge, and brings the current local-host bearer token back to the desktop through `run-as`, so the flow no longer needs manual token copy from the Connect tab each time.
- `pnpm android:local-host:smoke` 现在已经变成更真实的远端基线：如果 `/chat/send-wait` 返回 `state=error`、超时，或者 `/invoke` 返回 `ok=false`，脚本会非零退出并落盘 `summary.json`，同时回报 `chat.error_class`、`chat.error_host`、`chat.error_address_family` 与 `chat.hint`。`pnpm android:local-host:smoke` is now a more truthful remote baseline: if `/chat/send-wait` returns `state=error`, times out, or `/invoke` returns `ok=false`, the script exits non-zero, writes `summary.json`, and emits `chat.error_class`, `chat.error_host`, `chat.error_address_family`, and `chat.hint`.
- 2026 年 3 月 29 日在当前 OPPO / ColorOS 真机上的最新 adb-forward 复跑再次证明：`/status` 与 `device.status` 都正常，但聊天这段会稳定落到 `chat.error_class=openai_connect_timeout` 且 `chat.error_address_family=ipv6`；这说明当前 blocker 已经不是 token / 远控 write gate，而是手机到 `chatgpt.com` 的上游出站路径。The latest adb-forward rerun on March 29, 2026 on the current OPPO / ColorOS phone reconfirmed that `/status` and `device.status` are both fine, but the chat leg still lands at `chat.error_class=openai_connect_timeout` with `chat.error_address_family=ipv6`; that means the current blocker is no longer token or remote-write gating, but the phone's outbound path to `chatgpt.com`.
- 在这之上，现在仓库里还多了一条设备侧 OpenAI 网络探针：`pnpm android:local-host:openai-network`。它直接通过 adb 在手机上跑 `toybox nc -4/-6` 去测 `chatgpt.com` 和 `auth.openai.com` 的 `443` 连通性，并把 DNS 相关 `getprop` 和分类结果落进 `summary.json`。On top of that, the repo now also has a device-side OpenAI network probe as `pnpm android:local-host:openai-network`: it uses adb to run `toybox nc -4/-6` on the phone itself against `chatgpt.com` and `auth.openai.com` on `443`, then writes both the DNS-related `getprop` lines and the classified result into `summary.json`.
- 这条新探针在当前 OPPO / ColorOS 真机上的第一轮结果很关键：`chatgpt.com` 的 IPv4、IPv6 `443` 都 timeout，但 `auth.openai.com` 的 IPv4、IPv6 `443` 都是 reachable，因此当前边界已经进一步收敛成“Responses host path 不通”，而不是“整个 OpenAI 域都不通”。The first run of that new probe on the current OPPO / ColorOS phone is especially useful: both IPv4 and IPv6 `443` to `chatgpt.com` time out, while both IPv4 and IPv6 `443` to `auth.openai.com` are reachable, which narrows the boundary further to “the Responses host path is blocked” rather than “the whole OpenAI domain is down”.
- 现在还多了一条更顺手的一键诊断入口：`pnpm android:local-host:doctor`。它会优先复用现有 token；如果没有 token，就通过 trusted adb 自动 bootstrap debug token，然后顺序跑 `smoke`，并只在 `openai_connect_timeout` 时自动落到 `openai-network`，最终把三段结果合并进同一个 `summary.json`。There is now also a more convenient one-command diagnosis entrypoint as `pnpm android:local-host:doctor`: it prefers an existing token when present, otherwise bootstraps the debug token automatically over trusted adb, then runs `smoke`, and only falls through to `openai-network` when the failure is really `openai_connect_timeout`, finally combining all three stages into one `summary.json`.
- 这条 `doctor` 已经在当前 OPPO / ColorOS 真机上拿到第一条端到端证据：token bootstrap 成功，smoke 仅在 `chat` 段失败，随后自动 network probe 把顶层分类收敛成 `responses_host_unreachable`，推荐动作为 `check-chatgpt-path`。That `doctor` command already has its first real-device end-to-end proof on the current OPPO / ColorOS phone: token bootstrap succeeded, smoke failed only on the `chat` leg, and the automatic network probe then collapsed the top-level classification to `responses_host_unreachable` with the recommended action `check-chatgpt-path`.
- 本机 Host 聊天已接到使用 `gpt-5.4` 的 Codex Responses。Local-host chat is wired to Codex Responses using `gpt-5.4`.
- 当前本机 Host 聊天仍是“直接调用 Codex Responses”的形态，还没有接入桌面 Gateway 那套完整 agent/tool/plugin/runtime。The current local-host chat is still a direct Codex Responses flow and does not yet include the full desktop Gateway agent/tool/plugin/runtime stack.
- 已开始第一段桌面对齐能力：Codex Responses 现在按设计接入本机 `nodes` function-calling 闭环，可把一组 Android 原生命令作为聊天工具使用；当前进一步往“手机独立可用”方向推进，优先补相机 / 相册这类不依赖电脑的原生能力，同时本机 `workspace` 已扩展到搜索、替换、复制、移动等随身工作区能力，并在当前机器上完成了 Android-SDK-backed 单测复核。The first desktop-parity slice is now underway: Codex Responses is wired toward a local `nodes` function-calling loop so chat can use a set of Android native commands as tools; the current follow-up explicitly pushes toward phone-independent use by prioritizing native camera/photo capabilities that do not depend on a nearby computer, while the local `workspace` has also been expanded with search, replace, copy, and move capabilities and has now been re-verified with Android-SDK-backed unit tests on the current machine.
- 新增了 `Dedicated host deployment` 形态，用于闲置手机部署：它会把前台服务保活策略、开机 / 升级后恢复，以及本机 Host 掉线后的自愈重连接进 `Local Host` 模式。A new `Dedicated host deployment` mode now exists for idle-phone deployment: it wires keepalive behavior, boot/package-update restore, and self-heal reconnect back into `Local Host`.
- `Dedicated host deployment` 现在还会显式暴露 Android 电池优化状态，并把请求豁免的入口放进本机 Host 界面；同时 `device.status` 已开始回传 `backgroundExecution.batteryOptimizationIgnored`，方便远端检查部署 readiness。`Dedicated host deployment` now also exposes Android battery-optimization state directly in the local-host UI and links to the exemption flow; `device.status` also returns `backgroundExecution.batteryOptimizationIgnored` so remote tooling can inspect deployment readiness.
- 本机远控 `/status` 现在也会回传 `host.deployment`，包含 dedicated 模式、keepalive eligibility、电池优化豁免等 readiness 信号；另外前台服务被任务移除或销毁时，会在 dedicated 模式下排一个短延迟恢复闹钟。Remote `/status` now also returns `host.deployment`, including dedicated-mode, keepalive eligibility, and battery-optimization readiness signals; in addition, when the foreground service is task-removed or destroyed, dedicated mode now schedules a short recovery alarm.
- dedicated 模式现在还会维持一个低频 watchdog 闹钟，用来在长时间闲置场景下继续兜底前台服务恢复，而不仅仅依赖 task removed / destroy 的短恢复路径。Dedicated mode now also keeps a low-frequency watchdog alarm armed so long-idle phones still have a fallback path to restore the foreground service instead of depending only on the short task-removed / destroy recovery path.
- 电池优化豁免链路已经在当前真机上重新验证通过：系统弹窗可授予豁免，随后 `deviceidle whitelist` 和远端 `/status.host.deployment.batteryOptimizationIgnored` 都会同步变成 `true`。The battery-optimization exemption flow has now been revalidated on the current phone: the system confirmation dialog can grant the exemption, after which both `deviceidle whitelist` and remote `/status.host.deployment.batteryOptimizationIgnored` flip to `true`.
- 但同一台 OPPO / ColorOS V15 真机也暴露出另一条独立边界：把 `OpenClaw Node` 从 `Recents` 划掉会触发系统 `force stop` 并清空闹钟，即使已经拿到电池优化豁免也一样。But the same OPPO / ColorOS V15 phone also exposed a separate boundary: swiping `OpenClaw Node` away from Recents triggers a system `force stop` and clears alarms, even after the battery-optimization exemption is granted.
- 因此 dedicated deployment 现在会把 OEM 背景策略风险显式带到 UI 和 readiness 快照里，而不再假设“电池优化豁免”就代表闲置机部署已经稳妥。As a result, dedicated deployment now surfaces OEM background-policy risk explicitly in both the UI and the readiness snapshot instead of assuming the battery exemption alone makes idle-phone deployment safe.
- `pnpm android:local-host:dedicated:readiness` 这轮还开始直接回传 `recommendedAction` 和 `recommendedCommand`：它会根据当前 owner / accounts / DPC 状态，把下一步收敛到 `device-owner`、`testdpc-install`、`testdpc-qr` 或 `post-provision`，这样 dedicated 线后续也能像 codex guard 一样直接消费仓库命令，而不需要再手工读状态后自己拼流程。This round, `pnpm android:local-host:dedicated:readiness` also started returning `recommendedAction` and `recommendedCommand`: based on the current owner / accounts / DPC state, it now collapses the next step into `device-owner`, `testdpc-install`, `testdpc-qr`, or `post-provision`, so the dedicated-device lane can now consume repo commands directly the same way the codex guard path does instead of forcing the operator to rebuild the flow manually from raw state first.
- 真实 Android 手机已经完成 Codex 登录，并成功返回 GPT 回复。A real Android phone has completed Codex sign-in and returned a GPT response successfully.
- 远程访问已经暴露 `/health`、`/status`、`/examples`、`/chat/*`、`/events` 和 `/invoke`。Remote access exposes `/health`, `/status`, `/examples`, `/chat/*`, `/events`, and `/invoke`.
- 远程 `/invoke` 命令已经分层，高风险能力默认不开放。Remote invoke commands are tiered so risky actions are not enabled by default.
- 已有可执行的远程冒烟脚本，可直接验证 `/status`、聊天和 `/invoke`。A runnable remote smoke script exists for `/status`, chat, and `/invoke`.

Current boundaries / 当前边界:

- 旧的远控 smoke 最初主要是通过 LAN 跑通的，但这轮新的 `pnpm android:local-host:ui` 已经能在这台设备上通过 `adb forward` 成功复跑。The older remote smoke was first proven mainly over LAN, but in this round the new `pnpm android:local-host:ui` now replays successfully over `adb forward` on this device as well.
- 远程 API 目前适合工具接入，但还不是完整打磨过的远程控制产品。The remote API is usable for tooling, but it is not yet packaged as a polished remote-control product.
- 目前“很多桌面端能力在手机上不可用”仍然是预期范围内现象：Android `Local Host` 现在已经有 app-private workspace，也开始接入相机 / 相册等手机原生工具，但仍然没有完整 shell / browser / plugin runtime。It is still expected that many desktop capabilities are unavailable on the phone: Android `Local Host` now has an app-private workspace and is starting to expose native phone tools such as camera and photos, but it still does not bundle a full shell / browser / plugin runtime.
- 还没有在真机上强制制造一次“已登录但 token 真实过期”的失败记录；当前证据覆盖缺失授权和主动 refresh 成功。We have not yet forced a real "signed in but truly expired token" failure on-device; current evidence covers missing auth and successful proactive refresh.

## Latest Validation / 最新验证

### March 29, 2026 / 2026 年 3 月 29 日

- 同一天后续又把 launchd helper 收口成更接近“一键 setup”的入口：新增 `setup` 子命令，并把它做成幂等 orchestrator。它会先根据当前状态决定要不要写模板、要不要写 token、要不要 install / repair LaunchAgent，而不是继续要求外层自己串 `write-env -> install`。Later the same day, the launchd helper was further collapsed into a more nearly "one-command setup" entrypoint: a new `setup` subcommand now acts as an idempotent orchestrator. It decides whether to write a template, seed a token, or install / repair the LaunchAgent based on current state instead of forcing outer callers to keep chaining `write-env -> install` themselves.
- 与之配套的纯逻辑回归也已补进 `test/apps/android/local-host-codex-guard-launchd.test.ts`，覆盖了 fresh setup、token-seeded setup、repair-install，以及 already-healthy no-op 分支。Matching pure-logic regression coverage was also added to `test/apps/android/local-host-codex-guard-launchd.test.ts`, covering fresh setup, token-seeded setup, repair-install, and already-healthy no-op branches.
- 同一天后续又把 launchd helper 的首次安装体验继续推进：新增 `write-env` 子命令，默认会在 `~/.openclaw/android-local-host-codex-guard/guard.env` 写出模板，或者直接从 `OPENCLAW_ANDROID_LOCAL_HOST_TOKEN` / `--token` 写入真实 bearer token。Later the same day, the launchd helper's first-install experience moved forward again: a new `write-env` subcommand now writes the default template to `~/.openclaw/android-local-host-codex-guard/guard.env`, or seeds the real bearer token directly from `OPENCLAW_ANDROID_LOCAL_HOST_TOKEN` / `--token`.
- 同时 `status` 现在也会回传 `envFile`、`envFileExists` 和 `tokenConfigured`，所以桌面侧可以立刻看出当前是“尚未初始化 env”、“已有 env 但还是占位符”，还是“已写入真实 token”。At the same time, `status` now returns `envFile`, `envFileExists`, and `tokenConfigured`, so the desktop side can immediately tell whether the env file has not been initialized yet, still only contains the placeholder, or already has a real token.
- 在这之上，`status` 现在还会直接回传 `recommendedAction`，把下一步收敛成 `write-env`、`configure-token`、`install`、`check-launchagent` 或 `healthy`。On top of that, `status` now also returns `recommendedAction`, collapsing the next step into one of `write-env`, `configure-token`, `install`, `check-launchagent`, or `healthy`.
- 新增了 macOS guard 安装器 `apps/android/scripts/local-host-codex-guard-launchd.ts`，并把它接到 `pnpm android:local-host:codex-guard:launchd`。A new macOS guard installer `apps/android/scripts/local-host-codex-guard-launchd.ts` now exists and is wired into `pnpm android:local-host:codex-guard:launchd`.
- 这条 helper 默认安装一个 per-user LaunchAgent，运行的仍是现有 `codex-sync --watch` 守护逻辑，但 bearer token 改为从外部 env file 读取，而不是写进 LaunchAgent plist。The helper installs a per-user LaunchAgent that still runs the existing `codex-sync --watch` guard logic, but now reads the bearer token from an external env file instead of embedding it into the LaunchAgent plist.
- 同时 `apps/android/scripts/local-host-codex-sync.ts` 新增了 `--adb-bin`，这样 launchd wrapper 可以直接固定 `adb` 绝对路径，而不依赖 GUI 会话里的 PATH。At the same time, `apps/android/scripts/local-host-codex-sync.ts` gained `--adb-bin`, so the launchd wrapper can pin an absolute `adb` path instead of depending on the GUI session PATH.
- 这轮定向验证已通过：`pnpm test -- test/apps/android/local-host-codex-sync.test.ts test/apps/android/local-host-codex-guard-launchd.test.ts`、`pnpm android:local-host:codex-sync -- --help`、`pnpm android:local-host:codex-guard:launchd -- --help`、`pnpm android:local-host:codex-guard:launchd -- write-env --json`、用临时 state dir 运行的 `pnpm android:local-host:codex-guard:launchd -- setup --state-dir <tmp> --json`，以及更短 wrapper 的 `pnpm android:local-host:codex-guard:setup -- --help`、`pnpm android:local-host:codex-guard:status -- --json`。This round's targeted validation has already passed: `pnpm test -- test/apps/android/local-host-codex-sync.test.ts test/apps/android/local-host-codex-guard-launchd.test.ts`, `pnpm android:local-host:codex-sync -- --help`, `pnpm android:local-host:codex-guard:launchd -- --help`, `pnpm android:local-host:codex-guard:launchd -- write-env --json`, a temporary-state-dir run of `pnpm android:local-host:codex-guard:launchd -- setup --state-dir <tmp> --json`, and the shorter wrapper entries `pnpm android:local-host:codex-guard:setup -- --help` plus `pnpm android:local-host:codex-guard:status -- --json`.

### March 27, 2026 / 2026 年 3 月 27 日

- 这轮之后，`launch_app` 已正式以 `ui.launchApp` 形式落地：它采用 `packageName`-first 接口，走标准 Android launch intent，并为 “未安装” / “已安装但不可启动” 返回清晰错误。After this round, `launch_app` is now formally landed as `ui.launchApp`: it uses a `packageName`-first contract, launches through standard Android launch intents, and returns clear errors for "not installed" versus "installed but not launchable."
- `ui.launchApp` 已经接进协议常量、命令注册、调用分发、远端 write gate、`nodes` 工具桥接、Connect tab 示例和远端 `/examples`。`ui.launchApp` is now wired into the protocol constants, command registry, invoke dispatcher, remote write gate, the `nodes` tool bridge, the Connect-tab examples, and remote `/examples`.
- 同一天也把 `input_text` 落成实际命令 `ui.inputText`：它采用 focused-editable / selector-editable 的第一版边界，用 accessibility `ACTION_SET_TEXT` 写入文本，并且同样接进协议常量、命令注册、调用分发、远端 write gate、`nodes` 工具桥接、Connect tab 示例和远端 `/examples`。The same day also turned `input_text` into the real command `ui.inputText`: its first bounded form uses focused-editable / selector-editable targeting with accessibility `ACTION_SET_TEXT`, and it is wired into the protocol constants, command registry, invoke dispatcher, remote write gate, the `nodes` tool bridge, Connect-tab examples, and remote `/examples` too.
- 本轮定向验证已经通过：`ANDROID_HOME=\"$HOME/Library/Android/sdk\" ANDROID_SDK_ROOT=\"$HOME/Library/Android/sdk\" ./gradlew --no-daemon --console=plain :app:compileDebugKotlin` 成功，且 `UiAutomationHandlerTest`、`InvokeCommandRegistryTest`、`LocalHostNodesToolingTest`、`LocalHostRemoteAccessServerTest` 成功通过。This round's targeted verification has already passed: `ANDROID_HOME=\"$HOME/Library/Android/sdk\" ANDROID_SDK_ROOT=\"$HOME/Library/Android/sdk\" ./gradlew --no-daemon --console=plain :app:compileDebugKotlin` succeeded, and `UiAutomationHandlerTest`, `InvokeCommandRegistryTest`, `LocalHostNodesToolingTest`, and `LocalHostRemoteAccessServerTest` all passed.
- 真机补证也已完成一段：远端 `ui.launchApp(packageName=\"com.android.settings\")` 返回 `launched=true`，`adb shell dumpsys activity activities` 显示 `topResumedActivity=com.android.settings/.Settings`，而且 Settings 前台时远端 `/status` 还能立即继续回包。The device proof is also partially complete: remote `ui.launchApp(packageName=\"com.android.settings\")` returned `launched=true`, `adb shell dumpsys activity activities` showed `topResumedActivity=com.android.settings/.Settings`, and remote `/status` could still answer immediately while Settings stayed in the foreground.
- 这轮重新安装 APK 后，用 `settings put secure` 成功恢复了 OpenClaw 无障碍授权；随后远端 `/status` 和 `ui.state` 都重新回到 `uiAutomationAvailable=true`、`serviceConnected=true`、`available=true`。After reinstalling the APK in this round, we successfully restored the OpenClaw accessibility grant with `settings put secure`; afterward remote `/status` plus `ui.state` both returned to `uiAutomationAvailable=true`, `serviceConnected=true`, and `available=true`.
- `ui.inputText` 也已经拿到真机正证据：在 Connect 页远端调用 `ui.inputText(value=\"3945\", text=\"3945\", matchMode=\"exact\", packageName=\"ai.openclaw.app\")` 返回 `performed=true`、`strategy=selector_editable`，而且后续 `ui.state` 显示端口 `EditText` 仍是 `3945` 并进入 focused 状态。`ui.inputText` now has positive device proof too: on the Connect screen, remote `ui.inputText(value=\"3945\", text=\"3945\", matchMode=\"exact\", packageName=\"ai.openclaw.app\")` returned `performed=true` with `strategy=selector_editable`, and the follow-up `ui.state` showed the port `EditText` still set to `3945` and moved into focused state.
- 同一天晚些时候，新的 `pnpm android:local-host:ui` 也已经在真机通过：它通过 `adb forward` 成功完成 `ui.launchApp(OpenClaw)`、`ui.tap(Chat)`、`ui.waitForText(Select thinking level)`、`ui.tap(Type a message…)`、`ui.inputText(写入临时草稿)`、`ui.inputText(清空草稿)` 和最终 `ui.state`，并产出了 artifact。Later that same day, the new `pnpm android:local-host:ui` also passed on-device: over `adb forward` it successfully completed `ui.launchApp(OpenClaw)`, `ui.tap(Chat)`, `ui.waitForText(Select thinking level)`, `ui.tap(Type a message…)`, `ui.inputText` (write a temporary draft), `ui.inputText` (clear the draft), and the final `ui.state`, while producing artifacts.
- 现在又补了一条独立 cross-app probe：`apps/android/scripts/local-host-ui-cross-app-probe.sh` 和 `pnpm android:local-host:ui:cross-app` 会同时记录 ADB 前台 activity 真相、远端 `/status` 存活情况，以及 adb 把 OpenClaw 拉回前台后的恢复结果。2026 年 3 月 28 日，它已经在当前 OPPO / ColorOS 真机上给出第一条明确结论：目标 `com.android.settings` 被真正带到前台，5 秒观察窗口内 10 次 `/status` 探针全部成功，最终分类为 `foregrounded_host_reachable`，并且 adb 恢复后 `recovery.ok=true`。There is now also a separate cross-app probe: `apps/android/scripts/local-host-ui-cross-app-probe.sh` and `pnpm android:local-host:ui:cross-app` record the adb foreground-activity truth, remote `/status` reachability, and the post-recovery result after adb brings OpenClaw back. On March 28, 2026, it produced the first concrete result on the current OPPO / ColorOS phone: target `com.android.settings` truly reached the foreground, all 10 `/status` probes succeeded during the 5-second window, the run classified as `foregrounded_host_reachable`, and adb recovery ended with `recovery.ok=true`.
- 在此基础上，又补了一条多窗口 cross-app sweep：`apps/android/scripts/local-host-ui-cross-app-sweep.sh` 和 `pnpm android:local-host:ui:cross-app:sweep` 会顺序复用这条 probe，并把每档窗口的结论汇总成一份总 `summary.json`。2026 年 3 月 28 日默认 `5000,15000,30000` 三档 sweep 全部得到 `foregrounded_host_reachable`，顶层结果为 `allWindowsReachable=true`、`firstNonReachableWindowMs=null`，说明当前设置下 30 秒内还没有复现后台冻结边界。Building on that, there is now also a multi-window cross-app sweep: `apps/android/scripts/local-host-ui-cross-app-sweep.sh` and `pnpm android:local-host:ui:cross-app:sweep` reuse the probe sequentially and summarize every window into one top-level `summary.json`. On March 28, 2026, the default `5000,15000,30000` sweep produced `foregrounded_host_reachable` in all three windows, with top-level `allWindowsReachable=true` and `firstNonReachableWindowMs=null`, which means the background-freeze boundary was not reproduced within 30 seconds on the current setup.
- 为了把下一条“跨 app follow-up action”从临时 `curl` 收敛成仓库入口，这轮又把 `apps/android/scripts/local-host-ui-cross-app-probe.sh` 扩成了可选 follow-up harness：在设置环境变量后，它会在 reachability polling 之前额外执行 `ui.waitForText`、`ui.tap`、`ui.inputText` 和最终 `ui.state`。这条新路径目前还是 repo-side 脚手架，尚未补齐新的真机正证据，因此文档上仍把它记作“准备好可验证”，而不是“已验证通过”。To keep the next "cross-app follow-up action" from starting as ad-hoc `curl`, this round also expanded `apps/android/scripts/local-host-ui-cross-app-probe.sh` into an optional follow-up harness: once the env vars are set, it now executes `ui.waitForText`, `ui.tap`, `ui.inputText`, and a final `ui.state` before reachability polling begins. This new path is currently repo-side scaffolding only and does not yet have fresh positive device proof, so the docs still record it as "ready to validate" rather than "already validated."
- 这轮又把 app 内中英文切换继续往前推了一段：在原有 `AppLanguage` 持久化、`RootScreen` 语言上下文，以及 `Settings -> Language` 入口基础上，`OnboardingFlow` 已覆盖步骤条、权限页、Review 页和权限状态文案；同时 Chat / Voice 主界面、Voice 页 mic/runtime 细状态，以及 Voice 页新增暴露的 reply / TTS 细状态也已经接进同一条语言切换路径，`Codex auth` 的默认 OAuth 错误文案、登录提示页/登录状态文案、gateway auth/pairing 常见连接态、Chat 页常见失败提示、浏览器里的授权成功/失败页与回调错误页、Connect / onboarding 的 gateway 表单里那组 `Setup Code / Host / Token / Password` 高频静态文案、OAuth 上游动态失败、Codex Responses 的结构化请求/流式失败、远程访问启动失败常见原因、Voice talk 模式里的语音识别边缘错误、Chat error rail 上通用 `UNAVAILABLE:` / `INVALID_REQUEST:` 这类 RPC 前缀错误、本机 Host 的 `Local host error: ...` 连接失败状态、Voice 对话气泡里的 `Voice request failed` / `Response aborted` 终态失败文案，以及纯图片发送时 optimistic user message 里的 `See attached.` 占位文案也开始走同一套翻译。The in-app English / Simplified Chinese toggle moved forward again this round: on top of the persisted `AppLanguage`, the `RootScreen` language context, and the `Settings -> Language` entrypoint, `OnboardingFlow` now covers the step rail, permissions page, review page, and permission-status copy; the main Chat / Voice surfaces, the fine-grained mic/runtime status on the Voice screen, and the newly surfaced reply / TTS detail status on that same Voice screen are now on the same language-switching path too, while the default `Codex auth` OAuth errors, login prompt / login-state copy, common gateway auth/pairing connection states, common Chat-surface failures, the browser-side auth success/failure page plus callback-error page, the high-frequency `Setup Code / Host / Token / Password` gateway-form copy in Connect / onboarding, dynamic OAuth upstream failures, structured Codex Responses request / stream failures, common remote-access startup reasons, Voice talk-mode speech-recognizer edge errors, generic `UNAVAILABLE:` / `INVALID_REQUEST:`-style RPC-prefixed Chat error-rail failures, local-host `Local host error: ...` connection failures, the terminal `Voice request failed` / `Response aborted` copy inside Voice conversation bubbles, and the optimistic `See attached.` placeholder for attachment-only sends have also started using the same translation layer.
- 这条语言切换路径已经完成本地验证：`env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:compileDebugKotlin` 与 `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.auth.OpenAICodexAuthManagerTest --tests ai.openclaw.app.ui.AppStringsTest --tests ai.openclaw.app.ui.ConnectTabScreenStringsTest --tests ai.openclaw.app.SecurePrefsTest` 都成功通过；其中 `AppStringsTest`、`ConnectTabScreenStringsTest` 和 `OpenAICodexAuthManagerTest` 现在进一步锁住了 onboarding 手动配置次级文案、浏览器回调错误页、聊天页常见失败提示、连接/Voice runtime 状态翻译、gateway auth/pairing 常见连接态翻译、`Codex auth` 常见 OAuth 错误翻译，以及浏览器授权页的双语 CTA/提示。The language-toggle path has local verification too: `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:compileDebugKotlin` and `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.auth.OpenAICodexAuthManagerTest --tests ai.openclaw.app.ui.AppStringsTest --tests ai.openclaw.app.ui.ConnectTabScreenStringsTest --tests ai.openclaw.app.SecurePrefsTest` both passed; `AppStringsTest`, `ConnectTabScreenStringsTest`, and `OpenAICodexAuthManagerTest` now further lock onboarding manual-setup secondary copy, the browser callback error page, common Chat-surface failures, connection/Voice runtime translations, common gateway auth/pairing connection-state translations, common `Codex auth` OAuth-error translations, and the bilingual CTA/hint on the browser auth page into regression coverage.
- 最新一轮 gateway 表单文案收口还额外通过了 `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest`，把 `Setup Code / Host / Token / Password` 这组 helper 锁进了回归。The latest gateway-form copy cleanup also added another targeted verification pass through `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest`, locking the `Setup Code / Host / Token / Password` helper set into regression coverage.
- 最新一轮动态错误收口又额外通过了 `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.auth.OpenAICodexOAuthApiTest --tests ai.openclaw.app.auth.OpenAICodexAuthManagerTest --tests ai.openclaw.app.host.OpenAICodexResponsesClientTest --tests ai.openclaw.app.ui.AppStringsTest --tests ai.openclaw.app.ui.ConnectTabScreenStringsTest`，把结构化 OAuth 失败、Responses HTTP 失败、Responses `response.failed` SSE 失败，以及它们对应的中英文 UI 翻译一起锁进了回归。The latest dynamic-error cleanup also passed an extra targeted verification run via `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.auth.OpenAICodexOAuthApiTest --tests ai.openclaw.app.auth.OpenAICodexAuthManagerTest --tests ai.openclaw.app.host.OpenAICodexResponsesClientTest --tests ai.openclaw.app.ui.AppStringsTest --tests ai.openclaw.app.ui.ConnectTabScreenStringsTest`, locking structured OAuth failures, Responses HTTP failures, Responses `response.failed` SSE failures, and their matching bilingual UI translations into regression coverage.
- 最新一轮零散 UI 文案收口还额外通过了 `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest --tests ai.openclaw.app.ui.chat.ChatMarkdownStringsTest`，把 `Remote access failed to start: ...` 常见原因和 Markdown 图片占位的中英文 helper 一起锁进了回归。The latest loose-UI-copy cleanup also passed an extra targeted verification run via `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest --tests ai.openclaw.app.ui.chat.ChatMarkdownStringsTest`, locking the common `Remote access failed to start: ...` reasons and the Markdown image-placeholder bilingual helpers into regression coverage.
- 最新一轮 Voice 细状态补漏又额外通过了 `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest`，把 Talk Mode 里的 `Network error`、`Speech error (...)` 等语音识别边缘状态也锁进了同一套双语 helper。The latest Voice detail cleanup also passed an extra targeted verification run via `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest`, locking Talk Mode speech-recognizer edge states such as `Network error` and `Speech error (...)` into the same bilingual helper.
- 最新一轮 Chat / Local Host 错误补漏同样通过了 `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:compileDebugKotlin` 和 `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest`，把 `UNAVAILABLE: not connected`、`INVALID_REQUEST: message or attachment required`、`UI_AUTOMATION_UNAVAILABLE: ...` 以及 `Local host error: ...` 这一批主路径错误一起锁进了回归。The latest Chat / Local Host error cleanup also passed `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:compileDebugKotlin` plus `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest`, locking `UNAVAILABLE: not connected`, `INVALID_REQUEST: message or attachment required`, `UI_AUTOMATION_UNAVAILABLE: ...`, and the main `Local host error: ...` failures into regression coverage.
- 最新一轮 Voice / Chat 次级文案补漏也已经通过：`env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:compileDebugKotlin` 与 `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest --tests ai.openclaw.app.chat.ChatControllerStringsTest` 都成功，把 Voice 气泡里的 `Voice request failed` / `Response aborted` 以及 attachment-only `See attached.` 占位一起锁进了回归。The latest Voice / Chat secondary-copy cleanup also passed `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:compileDebugKotlin` plus `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest --tests ai.openclaw.app.chat.ChatControllerStringsTest`, locking the `Voice request failed` / `Response aborted` Voice-bubble copy and the attachment-only `See attached.` placeholder into regression coverage.
- 最新一轮 UI automation / permission 错误补漏也已经通过：`env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:compileDebugKotlin` 与 `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest` 都成功，把 `Contacts/Calendar/Location/Photos/SMS/Call Log/Motion` 这批剩余 `*_PERMISSION_REQUIRED` 错误，以及 `ui.launchApp` / `ui.waitForText` / `ui.tap` / `ui.inputText` 里常见的 app-launch / app-not-launchable / wait-timeout / target-mismatch / target-not-found / invalid-request / action-failed 失败原因一起锁进了双语 helper。The latest UI-automation / permission error cleanup also passed `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:compileDebugKotlin` plus `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.ui.AppStringsTest`, locking the remaining contacts/calendar/location/photos/SMS/call-log/motion `*_PERMISSION_REQUIRED` failures together with the common app-launch / app-not-launchable / wait-timeout / target-mismatch / target-not-found / invalid-request / action-failed reasons from `ui.launchApp` / `ui.waitForText` / `ui.tap` / `ui.inputText` into the bilingual helper.
- 这轮之后，这批最高频直出英文已经又少了两块；下一步应重新收敛到更少见的次级文案、零散连接边缘态，或者回到 cross-app follow-up 的真机正证据。After this round, two more of the highest-frequency English strings are gone; the next step should narrow back down to less-common secondary copy, scattered connection edge states, or a return to fresh on-device proof for the cross-app follow-up lane.
- 最新一轮定向补漏还把 `Codex auth` 的登录提示页和登录状态文案一起接进了双语路径，并通过 `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.auth.OpenAICodexAuthManagerTest` 复核。The latest targeted cleanup also routed the `Codex auth` login-prompt and login-state copy through the bilingual path and re-verified it with `env ANDROID_HOME=/Users/zouxiaoyi/Library/Android/sdk ANDROID_SDK_ROOT=/Users/zouxiaoyi/Library/Android/sdk ./gradlew :app:testDebugUnitTest --tests ai.openclaw.app.auth.OpenAICodexAuthManagerTest`.
- 针对闲置手机专机化，这轮又补了一条 dedicated readiness 探针：`apps/android/scripts/local-host-dedicated-readiness.sh` 和 `pnpm android:local-host:dedicated:readiness`。2026 年 3 月 28 日当前接入的 OPPO `PFEM10` 真机返回：`accountsCount=5`、`hasDeviceOwner=false`、`dpcInstalled=false`、`bootloaderLocked=true`，因此当前最合理的专机路线被收敛成 `device_owner_after_reset_or_account_cleanup`，而不是直接投入 root/systemize。For idle-phone dedicated deployment, this round also added a dedicated readiness probe: `apps/android/scripts/local-host-dedicated-readiness.sh` and `pnpm android:local-host:dedicated:readiness`. On March 28, 2026, the connected OPPO `PFEM10` phone returned `accountsCount=5`, `hasDeviceOwner=false`, `dpcInstalled=false`, and `bootloaderLocked=true`, which narrowed the most realistic dedicated-device path to `device_owner_after_reset_or_account_cleanup` instead of jumping directly into root/systemize.
- 这轮还把 dedicated 官方 QR 入管路径做成了仓库内工具：新增通用生成器 `apps/android/scripts/local-host-dedicated-provisioning-qr.ts` 和 `TestDPC` wrapper `pnpm android:local-host:dedicated:testdpc-qr`，可以直接拉取最新公开 `TestDPC` GitHub release、计算包校验和并输出 setup wizard 可扫的 QR。This round also turned the official dedicated-device QR provisioning lane into an in-repo tool: the new generic generator `apps/android/scripts/local-host-dedicated-provisioning-qr.ts` plus the `TestDPC` wrapper `pnpm android:local-host:dedicated:testdpc-qr` can fetch the latest public `TestDPC` GitHub release, compute the package checksum, and output a setup-wizard-scannable QR.
- 这轮还把 app 端的 lock-task 承接补上了：`apps/android/app/src/main/AndroidManifest.xml` 的 `MainActivity` 现在声明 `android:lockTaskMode="if_whitelisted"`，并且 `apps/android/app/src/main/java/ai/openclaw/app/MainActivity.kt` 会在 dedicated 模式、本机 Host 模式、onboarding 完成且 DPC 已 allowlist 本包时自动调用 `startLockTask()`。同时 `host.deployment` 里新增了 `lockTaskPermitted`、`lockTaskAutoEnterReady`、`lockTaskModeState`、`launcherActivityPresent`、`homeActivityPresent`。This round also wired the app side of lock-task handoff: `apps/android/app/src/main/AndroidManifest.xml` now declares `android:lockTaskMode="if_whitelisted"` on `MainActivity`, and `apps/android/app/src/main/java/ai/openclaw/app/MainActivity.kt` now calls `startLockTask()` automatically when dedicated mode, local-host mode, onboarding completion, and the DPC allowlist are all satisfied. At the same time, `host.deployment` now includes `lockTaskPermitted`, `lockTaskAutoEnterReady`, `lockTaskModeState`, `launcherActivityPresent`, and `homeActivityPresent`.
- 这轮又补了一条 post-provision checker：`apps/android/scripts/local-host-dedicated-post-provision-check.sh` 和 `pnpm android:local-host:dedicated:post-provision`。2026 年 3 月 28 日它在当前 OPPO `PFEM10` 真机上的 dry-run 与 `--launch` 结果都表明：`hasDeviceOwner=false`、`packageAllowlisted=false`、`lockTaskModeState=NONE`，但 app 自身已经是 `dedicatedEnabled=true`、`onboardingCompleted=true`、`gatewayConnectionMode=localHost`、`manifestLockTaskModeLabel=if_whitelisted`；因此 dedicated 的剩余差距已经被收敛成 DPC provisioning 与 lock-task allowlist，而不是 OpenClaw app 本身。This round also added a post-provision checker as `apps/android/scripts/local-host-dedicated-post-provision-check.sh` and `pnpm android:local-host:dedicated:post-provision`. On March 28, 2026, both its dry-run and `--launch` runs on the current OPPO `PFEM10` phone showed `hasDeviceOwner=false`, `packageAllowlisted=false`, and `lockTaskModeState=NONE`, while the app itself already had `dedicatedEnabled=true`, `onboardingCompleted=true`, `gatewayConnectionMode=localHost`, and `manifestLockTaskModeLabel=if_whitelisted`; that narrows the remaining dedicated-device gap to DPC provisioning plus the lock-task allowlist rather than the OpenClaw app itself.
- 这轮又把 DPC 侧的 kiosk 配置收敛成仓库命令：新增 `apps/android/scripts/local-host-dedicated-testdpc-kiosk.sh` 和 `pnpm android:local-host:dedicated:testdpc-kiosk`。它默认只做 dry-run，会复用 post-provision checker，并给出 `pm enable` 与 `am start ... KioskModeActivity` 的精确 adb 命令；只有显式传入 `--apply` 时才会真的触发 TestDPC kiosk 流程。This round also turned the DPC-side kiosk configuration into a repo command: the new `apps/android/scripts/local-host-dedicated-testdpc-kiosk.sh` plus `pnpm android:local-host:dedicated:testdpc-kiosk` default to dry-run mode, reuse the post-provision checker, and print the exact `pm enable` plus `am start ... KioskModeActivity` adb commands; they only trigger the real TestDPC kiosk flow when `--apply` is passed explicitly.
- 2026 年 3 月 28 日在当前 OPPO `PFEM10` 真机上的首次 `pnpm android:local-host:dedicated:testdpc-kiosk` dry-run 与 `--apply` 安全验证都表明：OpenClaw app 自身已经 ready，但 helper 会因为 `TestDPC is not installed` 与 `TestDPC is not the active Device Owner` 而停下，`applySucceeded=false`，不会在设备还没准备好时误改 kiosk 状态。The first `pnpm android:local-host:dedicated:testdpc-kiosk` dry-run and `--apply` safety check on March 28, 2026 on the current OPPO `PFEM10` phone both showed that the OpenClaw app itself is already ready, but the helper stops at `TestDPC is not installed` and `TestDPC is not the active Device Owner`, leaving `applySucceeded=false` so it does not mutate kiosk state before the device is ready.
- 为了把 `TestDPC is not installed` 这个 blocker 也收敛成仓库动作，这轮又新增了 `apps/android/scripts/local-host-dedicated-testdpc-install.sh` 和 `pnpm android:local-host:dedicated:testdpc-install`。它会抓取最新公开 TestDPC release、下载 APK，并在 dry-run 时产出精确的 `adb install -r -d ...` 命令；只有显式传入 `--apply` 时才会真的安装。To turn the `TestDPC is not installed` blocker into a repo action too, this round also added `apps/android/scripts/local-host-dedicated-testdpc-install.sh` plus `pnpm android:local-host:dedicated:testdpc-install`. It fetches the latest public TestDPC release, downloads the APK, and emits the exact `adb install -r -d ...` command in dry-run mode; it only installs for real when `--apply` is passed explicitly.
- 2026 年 3 月 28 日它在当前 OPPO `PFEM10` 真机上的首次 dry-run 已成功拉到 `v9.0.12` / `TestDPC_9.0.12.apk`，并确认设备当前仍是 `installed=false`、`device_owner=false`。这意味着 dedicated 主线现在已经不仅能指出 blocker，还能直接把“安装最新 TestDPC”这一步交给仓库命令。Its first dry-run on March 28, 2026 on the current OPPO `PFEM10` phone successfully fetched `v9.0.12` / `TestDPC_9.0.12.apk` and confirmed the device still reports `installed=false` and `device_owner=false`. That means the dedicated-device lane can now not only identify the blocker but also hand the "install the latest TestDPC" step to a repo command directly.
- 最新一轮又把 dedicated readiness 收口成更可执行的状态面：`apps/android/scripts/local-host-dedicated-readiness.sh` 与 `pnpm android:local-host:dedicated:readiness` 现在会把下一步直接写成 `recommendedAction` / `recommendedCommand`，并通过 `pnpm test -- test/apps/android/local-host-dedicated-readiness.test.ts` 锁住四种关键分支：`device-owner`、`testdpc-install`、`testdpc-qr` 和 `post-provision`。The latest round further collapsed dedicated readiness into a more actionable status surface: `apps/android/scripts/local-host-dedicated-readiness.sh` and `pnpm android:local-host:dedicated:readiness` now write the next step directly as `recommendedAction` / `recommendedCommand`, and `pnpm test -- test/apps/android/local-host-dedicated-readiness.test.ts` now locks the four key branches into regression coverage: `device-owner`, `testdpc-install`, `testdpc-qr`, and `post-provision`.
- 这轮又把 dedicated 线补成了更短的 dry-run 入口：新增 `apps/android/scripts/local-host-dedicated-next.sh` 和 `pnpm android:local-host:dedicated:next`。它会先跑 readiness，再自动执行当前 `recommendedAction` 对应的下一条仓库命令，并把 readiness + next-step 两段结果一起落成单个 summary artifact，因此 dedicated 线也开始拥有类似 codex guard `setup` 的单步仓库入口。This round also added a shorter dry-run entrypoint for the dedicated lane: the new `apps/android/scripts/local-host-dedicated-next.sh` plus `pnpm android:local-host:dedicated:next`. It runs readiness first, automatically executes the repo command for the current `recommendedAction`, and writes one combined summary artifact for both the readiness and next-step results, so the dedicated-device lane now also has a more single-step repo entry similar in spirit to codex guard `setup`.
- 这条新 wrapper 的定向验证也已经通过：`bash -n apps/android/scripts/local-host-dedicated-next.sh`、`pnpm test -- test/apps/android/local-host-dedicated-readiness.test.ts test/apps/android/local-host-dedicated-next.test.ts`、`pnpm android:local-host:dedicated:next -- --help` 和 `git diff --check` 都成功。Targeted verification for the new wrapper has also passed: `bash -n apps/android/scripts/local-host-dedicated-next.sh`, `pnpm test -- test/apps/android/local-host-dedicated-readiness.test.ts test/apps/android/local-host-dedicated-next.test.ts`, `pnpm android:local-host:dedicated:next -- --help`, and `git diff --check` all succeeded.
- 最新一轮又把 post-provision 本身收口成更可执行的状态面：`apps/android/scripts/local-host-dedicated-post-provision-check.sh` 与 `pnpm android:local-host:dedicated:post-provision` 现在也会把下一步直接写成 `recommendedAction` / `recommendedCommand`，并把后半段 dedicated 流程收敛到 `testdpc-qr`、`testdpc-kiosk`、`launch-openclaw` 或 `healthy` 这几种可消费分支。The latest round further collapsed post-provision itself into a more actionable status surface: `apps/android/scripts/local-host-dedicated-post-provision-check.sh` and `pnpm android:local-host:dedicated:post-provision` now also write the next step directly as `recommendedAction` / `recommendedCommand`, narrowing the second half of the dedicated-device flow into consumable branches such as `testdpc-qr`, `testdpc-kiosk`, `launch-openclaw`, or `healthy`.
- 这轮也给 dedicated 后半段补了更短的 dry-run 入口：新增 `apps/android/scripts/local-host-dedicated-post-provision-next.sh` 和 `pnpm android:local-host:dedicated:post-provision:next`。它会先跑 post-provision checker，再自动执行当前 `recommendedAction` 对应的安全下一步，并把 post-provision + next-step 两段结果一起落成单个 summary artifact，因此 dedicated 线在进入 post-provision 阶段后，也开始拥有类似 `dedicated:next` 的单步仓库入口。This round also added a shorter dry-run entrypoint for the second half of the dedicated lane: the new `apps/android/scripts/local-host-dedicated-post-provision-next.sh` plus `pnpm android:local-host:dedicated:post-provision:next`. It runs the post-provision checker first, automatically executes the safe next step for the current `recommendedAction`, and writes one summary artifact for both the post-provision plus next-step results, so once the dedicated lane reaches post-provision it also gains a more single-step repo entry similar to `dedicated:next`.
- 这条新 wrapper 的定向验证也已经通过：`bash -n apps/android/scripts/local-host-dedicated-post-provision-check.sh apps/android/scripts/local-host-dedicated-post-provision-next.sh`、`pnpm test -- test/apps/android/local-host-dedicated-post-provision-next.test.ts`、`pnpm android:local-host:dedicated:post-provision:next -- --help` 和 `git diff --check` 都成功。Targeted verification for the new wrapper has also passed: `bash -n apps/android/scripts/local-host-dedicated-post-provision-check.sh apps/android/scripts/local-host-dedicated-post-provision-next.sh`, `pnpm test -- test/apps/android/local-host-dedicated-post-provision-next.test.ts`, `pnpm android:local-host:dedicated:post-provision:next -- --help`, and `git diff --check` all succeeded.
- cross-app follow-up harness 这轮也补上了第一条内置 preset：`OPENCLAW_ANDROID_LOCAL_HOST_UI_CROSS_APP_PRESET=settings-search-input`。在真实 OPPO / ColorOS 中文设置页上复跑后，已经把它从依赖英文 `Settings / Search` 文案，收成了优先使用稳定 `resourceId` 的 `tap(com.android.settings:id/searchView) -> input(com.android.settings:id/search_src_text, openclaw)` 复跑链；显式 env override 依然保持最高优先级。This round also added the first built-in preset for the cross-app follow-up harness as `OPENCLAW_ANDROID_LOCAL_HOST_UI_CROSS_APP_PRESET=settings-search-input`. After rerunning it against the real OPPO / ColorOS Chinese Settings surface, it has now been tightened from depending on English `Settings / Search` copy into a stable `resourceId` chain of `tap(com.android.settings:id/searchView) -> input(com.android.settings:id/search_src_text, openclaw)`, while explicit env overrides still keep the highest priority.
- cross-app follow-up 现在还多了一个更直接的 repo 入口：`pnpm android:local-host:ui:cross-app:next`。它默认就会带上当前 preset `settings-search-input`，并把 describe 元数据和最终 probe/sweep 结果汇总进同一个 `next-summary.json`，所以这条线已经从“准备好可验证”进一步收敛成了“有默认 next 命令可跑”。There is now also a more direct repo entrypoint for the cross-app follow-up lane as `pnpm android:local-host:ui:cross-app:next`. It defaults the current `settings-search-input` preset and combines the describe metadata plus the final probe/sweep result into one `next-summary.json`, so the lane has tightened from "ready to validate" into "has a default next command to run."
- 这轮还补了 `pnpm android:local-host:token` 这条 debug-only adb token bootstrap：它会启动 app、触发 app 进程里的调试导出桥，再通过 `run-as` 读回当前 bearer token，所以本地 `adb` 验证不再必须每次手动回到 Connect tab 抄 token。This round also added `pnpm android:local-host:token` as a debug-only adb token bootstrap: it launches the app, triggers the in-process debug export bridge, then reads the current bearer token back through `run-as`, so local adb validation no longer has to manually revisit the Connect tab and re-copy the token every time.
- 在 2026 年 3 月 29 日晚上的同一台 OPPO / ColorOS 真机上，这条新 token helper 已经成功导出当前 bearer token；随后用它立刻重跑 `pnpm android:local-host:ui:cross-app:next`，新的 resource-id preset 也直接得到 `classification=foregrounded_host_reachable`、`tap_ok=true`、`input_ok=true`、`state_ok=true`。On the same OPPO / ColorOS device on the evening of March 29, 2026, the new token helper successfully exported the current bearer token; we then immediately reran `pnpm android:local-host:ui:cross-app:next` with it, and the new resource-id preset returned `classification=foregrounded_host_reachable` with `tap_ok=true`, `input_ok=true`, and `state_ok=true`.
- 这一轮又把 `ui.swipe` 正式接进 Android UI 自动化命令面：第一版采用坐标驱动的 `startX/startY/endX/endY` 接口，支持可选 `durationMs` 与 `packageName` 守卫，同步接入了 write tier、nodes tooling、`/examples`、Connect tab 文案和中英文本地化错误；本地 `:app:compileDebugKotlin` 与定向 `UiAutomationHandlerTest` / `LocalHostNodesToolingTest` / `LocalHostRemoteAccessServerTest` / `InvokeCommandRegistryTest` / `AppStringsTest` 也都已通过。This round also officially wired `ui.swipe` into the Android UI-automation command surface: the first version uses a coordinate-driven `startX/startY/endX/endY` contract with optional `durationMs` and `packageName` guarding, and it is now wired through the write tier, nodes tooling, `/examples`, Connect-tab copy, and bilingual localized errors; local `:app:compileDebugKotlin` and the targeted `UiAutomationHandlerTest` / `LocalHostNodesToolingTest` / `LocalHostRemoteAccessServerTest` / `InvokeCommandRegistryTest` / `AppStringsTest` all pass as well.
- 随后又发现一条真实的 repo-side race：`pnpm android:local-host:ui:cross-app:next` 在 `ui.launchApp(com.android.settings)` 之后太快开始做 follow-up，会命中 `UI_TARGET_MISMATCH: Active package ai.openclaw.app does not match com.android.settings`。这轮已把 `apps/android/scripts/local-host-ui-cross-app-probe.sh` 修成先轮询 `ui.state`，等目标 package 真正变成 active window，再开始 `ui.tap` / `ui.inputText`；对应 `bash -n` 与 `pnpm test -- test/apps/android/local-host-ui-cross-app-probe.test.ts test/apps/android/local-host-ui-cross-app-next.test.ts` 已通过。We then found a real repo-side race: `pnpm android:local-host:ui:cross-app:next` was starting its follow-up too quickly after `ui.launchApp(com.android.settings)`, which hit `UI_TARGET_MISMATCH: Active package ai.openclaw.app does not match com.android.settings`. This round fixes `apps/android/scripts/local-host-ui-cross-app-probe.sh` to poll `ui.state` first and only begin `ui.tap` / `ui.inputText` once the target package truly becomes the active window; the matching `bash -n` plus `pnpm test -- test/apps/android/local-host-ui-cross-app-probe.test.ts test/apps/android/local-host-ui-cross-app-next.test.ts` checks now pass.
- 用最新 debug APK 重新安装到当前 OPPO / ColorOS 真机后，`/status` 与 `/invoke/capabilities` 已确认设备侧也真的带上 `ui.swipe`；同一轮复跑 `pnpm android:local-host:ui:cross-app:next` 再次通过，并在 summary 里显式给出 `foreground_ready=true`、`tap_ok=true`、`input_ok=true`、`state_ok=true`。After reinstalling the latest debug APK onto the current OPPO / ColorOS phone, `/status` and `/invoke/capabilities` confirmed that the device-side surface really includes `ui.swipe` as well; the same round reran `pnpm android:local-host:ui:cross-app:next` successfully again, now explicitly reporting `foreground_ready=true`, `tap_ok=true`, `input_ok=true`, and `state_ok=true` in the summary.
- 这轮还补上了第一条 `ui.swipe` 真机正证据：先强制重启 `com.android.settings` 回到设置首页，确认滑动前 `visibleText` 里仍有 `WLAN` / `蓝牙` / `移动网络` / `连接与共享` / `桌面与锁屏`，再执行 `ui.swipe(startX=720,startY=2600,endX=720,endY=900,durationMs=350,packageName=com.android.settings)`；滑动后 active package 仍是 `com.android.settings`，同时 `visibleText` 明确切换到 `通知与控制中心` / `密码与安全` / `隐私` / `应用` / `电池` 这一组下层设置项。This round also added the first positive on-device proof for `ui.swipe`: after force-restarting `com.android.settings` back to the Settings homepage and confirming the pre-swipe `visibleText` still contained `WLAN`, `蓝牙`, `移动网络`, `连接与共享`, and `桌面与锁屏`, we executed `ui.swipe(startX=720,startY=2600,endX=720,endY=900,durationMs=350,packageName=com.android.settings)`; afterward the active package still remained `com.android.settings`, while `visibleText` clearly shifted to lower-level Settings items such as `通知与控制中心`, `密码与安全`, `隐私`, `应用`, and `电池`.
- 但同一轮也暴露了新的 OEM 背景风险：在当前 OPPO / ColorOS 手机上，OpenClaw 退到后台后会被 `OplusHansManager` 冻住，导致后续远端请求超时；这意味着 `ui.launchApp` 已经不是“不会离开 app”，而是“离开 app 后的持续控制还会被 OEM 背景策略打断”。But the same run also exposed a new OEM background risk: on the current OPPO / ColorOS phone, once OpenClaw moves into the background it can be frozen by `OplusHansManager`, causing subsequent remote requests to time out; this means `ui.launchApp` is no longer blocked on "cannot leave the app," but longer-lived follow-up control can still be interrupted by OEM background policy.

### March 26, 2026 / 2026 年 3 月 26 日

- 提交 `170f8a125b` 已把 `ui.tap`、`ui.back`、`ui.home` 正式挂到远端 write tier，并同步更新了 Connect tab 文案与 `/examples` 里的 gated `ui.tap` 示例。Commit `170f8a125b` now places `ui.tap`, `ui.back`, and `ui.home` behind the remote write tier and also updates the Connect-tab copy plus the gated `ui.tap` example in `/examples`.
- 真机复核时先发现设备 `base.apk` 仍是旧构建：本地 APK 已包含新的 `invoke-ui-tap` 和 bounded-UI 文案，但设备上的 `base.apk` 还没有；重新执行 `adb install -r -d apps/android/app/build/outputs/apk/debug/openclaw-2026.3.14-debug.apk` 后，设备 APK 内容才与本地构建对齐。During the device recheck, we first found that the phone's `base.apk` was still an older build: the local APK already contained the new `invoke-ui-tap` and bounded-UI strings, but the device `base.apk` did not; only after rerunning `adb install -r -d apps/android/app/build/outputs/apk/debug/openclaw-2026.3.14-debug.apk` did the device APK align with the local build.
- 重新安装后的远端 `/status` 与 `/invoke/capabilities` 已再次验证：write 关闭时 allowlist 仍只有 `ui.state` 与 `ui.waitForText`，而在设备上开启 write tier 后，才会出现 `ui.tap`、`ui.back`、`ui.home`。After reinstalling, remote `/status` plus `/invoke/capabilities` were revalidated again: with write still disabled the allowlist only exposes `ui.state` and `ui.waitForText`, and only after enabling the write tier on-device do `ui.tap`, `ui.back`, and `ui.home` appear.
- 当前 OPPO / ColorOS 真机在重新安装 APK 后会把 OpenClaw accessibility grant 清空；重新开启无障碍服务后，`ui.state` 恢复到 `enabled=true`、`serviceConnected=true`、`available=true`。On the current OPPO / ColorOS phone, reinstalling the APK clears the OpenClaw accessibility grant; once the accessibility service is re-enabled, `ui.state` returns to `enabled=true`, `serviceConnected=true`, and `available=true`.
- 第一条真机 observe / wait / act 闭环现已成立：`ui.tap(text=\"Chat\")` 成功把底部标签切到 Chat，随后 `ui.home` 与 `ui.back` 都能把活动 `packageName` 切到 `com.android.launcher`。The first real-device observe / wait / act loop is now live: `ui.tap(text=\"Chat\")` successfully switches the bottom tab to Chat, and `ui.home` plus `ui.back` both move the active `packageName` to `com.android.launcher`.
- “怎么操控手机”的调研结论保持不变，并继续以 `apps/android/local-host-ui-automation-plan.md` 为主文档：OpenClaw Android 的主路线仍是 app 内 `AccessibilityService`，而不是把 ADB / Appium / `Open-AutoGLM` 直接当运行时架构。The phone-control research conclusion remains unchanged and continues to live primarily in `apps/android/local-host-ui-automation-plan.md`: the main OpenClaw Android path is still an in-app `AccessibilityService`, not directly adopting ADB / Appium / `Open-AutoGLM` as the runtime architecture.

### March 23, 2026 / 2026 年 3 月 23 日

- [x] 安装 Java 17，并补齐 Android SDK command-line tools、`platforms;android-36`、`build-tools;36.0.0`、`platform-tools`。Installed Java 17 and the Android SDK command-line tools plus `platforms;android-36`, `build-tools;36.0.0`, and `platform-tools`.
- [x] 运行 `./gradlew --no-daemon --console=plain :app:assembleDebug` 并成功生成 debug APK。Ran `./gradlew --no-daemon --console=plain :app:assembleDebug` and produced the debug APK successfully.
- [x] 通过 `adb install -r -d apps/android/app/build/outputs/apk/debug/openclaw-2026.3.14-debug.apk` 安装到一台真实 Android 15 设备。Installed the debug APK onto a real Android 15 device with `adb install -r -d apps/android/app/build/outputs/apk/debug/openclaw-2026.3.14-debug.apk`.
- [x] 运行 `./gradlew --no-daemon --console=plain :app:testDebugUnitTest --tests ai.openclaw.app.SecurePrefsTest --tests ai.openclaw.app.host.LocalHostRuntimeTest --tests ai.openclaw.app.host.LocalHostRemoteAccessServerTest` 并通过。Ran `./gradlew --no-daemon --console=plain :app:testDebugUnitTest --tests ai.openclaw.app.SecurePrefsTest --tests ai.openclaw.app.host.LocalHostRuntimeTest --tests ai.openclaw.app.host.LocalHostRemoteAccessServerTest` successfully.
- [x] 在真机上绕过首次 onboarding，进入 `Local Host` 模式，并看到 `Connected` 状态。Bypassed first-run onboarding on the phone, entered `Local Host`, and observed the `Connected` state.
- [x] 远控 LAN 地址 `http://<phone-ip>:3945` 返回 `/status`、`/invoke/capabilities` 和 `device.status`。The LAN remote-access URL `http://<phone-ip>:3945` returned `/status`, `/invoke/capabilities`, and `device.status`.
- [x] `/chat/send-wait` 已返回清晰错误 `OpenAI Codex login required`，证明本机聊天和错误通路都已接通。`/chat/send-wait` returned the clear error `OpenAI Codex login required`, proving the local chat path and failure path are wired correctly.
- [x] Codex 浏览器授权已完成，`/status` 返回 `codexAuthConfigured=true`。Codex browser authorization completed, and `/status` returned `codexAuthConfigured=true`.
- [x] 重新安装并重启 App 后，远控 `/chat/send-wait` 成功返回 `Android local host is working.`。After reinstalling and restarting the app, remote `/chat/send-wait` successfully returned `Android local host is working.`.
- [x] 运行 `bash apps/android/scripts/local-host-remote-smoke.sh`（经 LAN 访问）成功，返回 `Android local host smoke passed.`。Ran `bash apps/android/scripts/local-host-remote-smoke.sh` over LAN successfully and received `Android local host smoke passed.`.
- [x] 远控 `/invoke` 的安全边界已在真机验证：`camera.snap` 与 `sms.send` 在对应命令层关闭时返回 `command is not enabled for remote access`。Remote `/invoke` safety boundaries were validated on-device: `camera.snap` and `sms.send` returned `command is not enabled for remote access` while their command tiers were disabled.
- [x] 无效 bearer token 调用 `/status` 返回 `401` 和 `Missing or invalid bearer token`。Calling `/status` with an invalid bearer token returned `401` and `Missing or invalid bearer token`.
- [x] 新增并验证 `GET /api/local-host/v1/auth/codex/status`，可返回脱敏后的授权状态与过期时间。Added and validated `GET /api/local-host/v1/auth/codex/status`, which returns sanitized auth metadata and expiry information.
- [x] 新增并验证 `POST /api/local-host/v1/auth/codex/refresh`，真机返回 `refreshed=true`，且 `expiresAt` 前移。Added and validated `POST /api/local-host/v1/auth/codex/refresh`; on-device it returned `refreshed=true`, and `expiresAt` moved forward.
- [x] 主动 refresh 之后，远控 `/chat/send-wait` 仍成功返回 `Codex refresh still works.`。After the proactive refresh, remote `/chat/send-wait` still returned `Codex refresh still works.` successfully.
- [x] 新增并运行 `bash apps/android/scripts/local-host-permission-smoke.sh`，覆盖 `contacts.search`、`calendar.events`、`photos.latest`、`system.notify` 的权限缺失错误。Added and ran `bash apps/android/scripts/local-host-permission-smoke.sh`, covering permission-missing errors for `contacts.search`, `calendar.events`, `photos.latest`, and `system.notify`.

## Session Log / 会话日志

### March 25, 2026 / 2026 年 3 月 25 日

- 对照代码重新复核了 Android 本机 Host 远程访问默认值：远程访问默认关闭，端口默认为 `3945`，高风险命令层默认关闭，而且 Connect tab 已支持 bearer token 再生成。Re-reviewed the Android local-host remote-access defaults against code: remote access is off by default, the default port is `3945`, higher-risk command tiers default to off, and the Connect tab already supports bearer-token regeneration.
- 在 `apps/android/README.md` 补齐了远程访问说明，明确只推荐 `LAN` / trusted tunnel、先探测 `/status`、用 `auth/codex/refresh` 维护 Codex 授权、以及在需要撤销访问时单独轮换远程 bearer token。Documented the remote-access guidance in `apps/android/README.md`, explicitly recommending only `LAN` / trusted tunnels, probing `/status` first, using `auth/codex/refresh` for Codex auth freshness, and rotating the remote bearer token separately when access should be revoked.
- 重新跑通了与收尾判断直接相关的 Android 单测：`LocalHostRuntimeTest`、`LocalHostRemoteAccessServerTest`、`LocalHostNodesToolingTest`、`LocalHostWorkspaceToolingTest`、`InvokeCommandRegistryTest`，结果 `BUILD SUCCESSFUL`。Reran the Android unit tests most relevant to the close-out judgment: `LocalHostRuntimeTest`, `LocalHostRemoteAccessServerTest`, `LocalHostNodesToolingTest`, `LocalHostWorkspaceToolingTest`, and `InvokeCommandRegistryTest`, and the result was `BUILD SUCCESSFUL`.
- 基于当前目标与代码边界，决定把 MVP 远程命令面收口为：默认只读远控集 + 可选相机高级层 + 可选写操作层 + 共享 write gate 的 workspace 写能力；不再为当前 MVP 继续扩命令。Based on the current goal and code boundaries, the MVP remote-command surface is now considered frozen as: the default read-only remote set plus the optional camera advanced tier, the optional write tier, and workspace writes that share the existing write gate; no further command expansion is planned for the current MVP.
- 同时保留一个明确的真机 blocker：代码和单测已经证明流式 delta 会从 `OpenAICodexResponsesClient` 进入 `LocalHostRuntime`、`ChatController` 和聊天流式气泡，但自检要求直接证据，所以仍需要一次真机记录才能把 streaming 项打勾。At the same time, one explicit real-device blocker remains: code and unit tests now confirm streaming deltas flow from `OpenAICodexResponsesClient` through `LocalHostRuntime`, `ChatController`, and the chat streaming bubble, but the self-check requires direct evidence, so one on-device capture is still needed before the streaming item can be checked off.
- 为这个 blocker 新增了 `apps/android/scripts/local-host-streaming-smoke.sh`，并通过 `pnpm android:local-host:streaming` 暴露出来：它会用 `/chat/send` + `/events` 追踪同一条 run，要求在 `final` 前至少看到一次 `state=delta`。Added `apps/android/scripts/local-host-streaming-smoke.sh` for this blocker and exposed it as `pnpm android:local-host:streaming`: it tracks the same run through `/chat/send` plus `/events` and requires at least one `state=delta` before `final`.
- 真机补证时，先发现手机当前网络对 `chatgpt.com:443` 直连超时；主机不走系统代理时对同一域名也会超时，所以最初的 streaming 失败先被确认成网络前置条件，而不是 Android 本机 Host 回归。During the real-device retry, we first found that the phone's current network timed out on direct `chatgpt.com:443` access; the host also timed out on the same domain when not using its system proxy, so the initial streaming failure was confirmed as a network precondition issue rather than an Android local-host regression.
- 随后把手机临时切到一条可信 LAN 代理路径，经主机系统代理成功打通外网出口，证明设备已经能够真正到达 Codex 服务侧。We then temporarily routed the phone through a trusted LAN proxy path and successfully restored external egress through the host's system proxy, proving the device could genuinely reach the Codex service side.
- 同一天更早的一次重跑 `pnpm android:local-host:streaming` 曾在这条可信代理路径上立即返回 `The usage limit has been reached | errorType=usage_limit_reached`，而不是网络或本机错误；当时它说明剩余 blocker 已经收敛成“缺可用额度下的一次正向真机证据”，不是实现路径未通。An earlier same-day rerun of `pnpm android:local-host:streaming` immediately returned `The usage limit has been reached | errorType=usage_limit_reached` on that trusted proxy path rather than a network or local-host error; at that point it showed the remaining blocker had narrowed to "one positive on-device capture with available account usage," not a missing implementation path.
- 在补充 Codex 额度后，再次经同一条可信代理路径重跑真机 streaming：先用较长 prompt 在原始 `/events` 中观察到同一条 run 持续产出 `chat state=delta`，并最终落到 `state=final`；随后用较短的 8-line prompt 重跑 `pnpm android:local-host:streaming`，脚本直接通过，记录到 `deltaCount=68`、`terminalState=final`。After Codex usage was topped up, we reran real-device streaming over the same trusted proxy path: first a longer prompt showed repeated `chat state=delta` events for the same run in raw `/events` and eventually reached `state=final`; then a shorter 8-line prompt made `pnpm android:local-host:streaming` pass directly, recording `deltaCount=68` and `terminalState=final`.
- 据此，streaming 直证已在真机上补齐；而“已登录但真过期 token”的特意构造复现被降级为可选加固，不再视为当前 MVP 收尾 blocker，因为缺失授权的清晰错误、主动 refresh、以及 `auth/codex/status` 可见性已经把操作恢复路径覆盖到位。As a result, streaming direct evidence is now complete on-device; a deliberately forced "signed in but truly expired token" repro is downgraded to optional hardening rather than an MVP close-out blocker because the clear missing-auth error, proactive refresh, and `auth/codex/status` visibility already cover the operator recovery path.
- 又尝试修正了 Codex 浏览器授权完成后的回 App 链路：新增了 `openclaw://auth/callback` deep link、浏览器成功页的 `Return to OpenClaw` CTA，以及对应单测和真机 deep-link resolve 验证；但在当前 OPPO / ColorOS 真机上，真实浏览器授权结束后仍不能稳定自动回到 App。当前先把它记成已知问题，继续依赖“手动切回 App / 粘贴 redirect URL 或 code”的兜底路径，不阻塞后续 phone-control 方向推进。We also tried to fix the Codex browser-auth return-to-app flow by adding the `openclaw://auth/callback` deep link, a `Return to OpenClaw` CTA on the browser success page, and the related unit-test plus device-side deep-link resolution checks; however, on the current OPPO / ColorOS phone the real browser auth flow still does not reliably return to the app afterward. For now this is recorded as a known issue, and we continue relying on the fallback path of manually switching back to the app or pasting the redirect URL / code so phone-control work is not blocked.
- 按“先推进手机操控”这个方向，又补做了一轮 GitHub 和官方文档调研：Z.ai 最近最值得参考的是 `Open-AutoGLM`，但它本质上是 Python + 模型端点 + ADB / remote ADB 的外部主控架构；对 OpenClaw Android 来说，下一阶段仍应优先做本机 `AccessibilityService` 能力面，并从第一天起把 sideload / internal 路线与 Google Play 分发边界分开看待。To support the "move phone control forward first" direction, we also did another round of GitHub plus official-doc research: the most relevant recent Z.ai project is `Open-AutoGLM`, but it is fundamentally an external-controller architecture built around Python plus model endpoints plus ADB / remote ADB; for OpenClaw Android, the next phase should still prioritize an on-device `AccessibilityService` capability surface, while treating sideload / internal deployment and Google Play distribution as separate lanes from day one.
- 已经开始落第一段 UI 自动化骨架：新增 Android `AccessibilityService` 声明与 XML 配置，加入 app 内 readiness 快照与系统设置入口，并让本机 Host `/status` 开始返回 `uiAutomationAvailable` 和详细 `uiAutomation` 对象；Connect tab 现在也能直接引导用户开启无障碍服务。The first UI-automation scaffolding slice has now landed: Android `AccessibilityService` declaration plus XML config, an app-side readiness snapshot and system-settings entrypoint, and Local Host `/status` now returning `uiAutomationAvailable` plus a detailed `uiAutomation` object; the Connect tab can now guide the user directly to enable accessibility access.
- 又继续把第一条只读 UI 观察链路接上：新增 `ui.state` 命令，远端和本机都能在服务未开启时拿到结构化 readiness 原因，在服务已连上时拿到当前活动窗口的 `packageName`、`visibleText`、`nodeCount` 和扁平节点摘要。We then pushed the first read-only UI-observation path through: a new `ui.state` command now lets both local and remote callers get a structured readiness reason while the service is disabled, and once the service is live it returns the active window's `packageName`, `visibleText`, `nodeCount`, and a flattened node summary.
- 因此下一步已经从“做 readiness”切换成“把 `ui.state` 从扁平快照继续推进到 selector / wait / bounded actions”，优先级应是 `wait_for_text`、`tap`、`back`、`home`，而不是再补更多状态牌。So the next step has now moved beyond readiness and into extending `ui.state` toward selectors, waits, and bounded actions; the next priorities should be `wait_for_text`, `tap`, `back`, and `home` rather than adding more status cards.

### March 24, 2026 / 2026 年 3 月 24 日

- 开始把 Android `Local Host` 从“纯文本 Codex 聊天”推进到“Codex + 原生工具调用”形态。Started moving Android `Local Host` from plain-text Codex chat toward a `Codex + native tool-calling` shape.
- 新增本机 `nodes` 工具桥接与共享命令策略，把聊天里的工具调用接到 Android 原生 `/invoke` 命令面。Added a local `nodes` tool bridge plus shared command policy so chat-time tool calls can execute through the Android native `/invoke` surface.
- 远程会话会复用现有命令层级，不允许通过聊天工具调用绕过只读 / 高级 / 写操作开关。Remote sessions now reuse the existing command tiers so chat tool-calling cannot bypass the read-only / advanced / write gates.
- 本机 Host 运行时已开始发出 `agent.stream=tool` 事件，聊天 UI 可以沿用现有 pending-tool bubble 展示工具执行过程。The local-host runtime now emits `agent.stream=tool` events so the chat UI can reuse the existing pending-tool bubble for tool execution progress.
- 在这条基础上继续向“手机独立使用”推进：把相机列表、相机拍照、最近照片读取纳入聊天工具面，并把图片结果改造成 Responses 可直接理解的 `input_image` 回灌。Built on top of that toward more independent phone use: camera listing, camera snap, and recent-photo access are being added to the chat tool surface, and image results are rewritten into Responses-compatible `input_image` follow-up inputs.
- 新增 app-private `workspace` 工具，允许模型在手机本地沙箱里列目录、读写文本、创建目录、删除文件，把“记笔记/写草稿/落中间产物”这类能力真正搬到手机上。Added an app-private `workspace` tool so the model can list directories, read/write text, create folders, and delete files inside an on-device sandbox, bringing note-taking, drafting, and intermediate artifact storage onto the phone itself.
- 本机 `workspace` 进一步新增搜索、替换、复制、移动能力，并把远程聊天下的 workspace 写操作接到与 `/invoke` 一致的写权限开关上，避免远端经聊天工具绕过原有边界。The local `workspace` has been extended with search, replace, copy, and move capabilities, and remote-chat workspace writes now share the same write-enabled gate as `/invoke` so remote sessions cannot bypass existing boundaries through chat tools.
- 在当前机器上补齐 Android SDK 后，已成功运行 `LocalHostWorkspaceToolingTest`、`LocalHostNodesToolingTest`、`LocalHostRuntimeTest`、`LocalHostRemoteAccessServerTest`、`OpenAICodexResponsesClientTest`。After installing the Android SDK on the current machine, `LocalHostWorkspaceToolingTest`, `LocalHostNodesToolingTest`, `LocalHostRuntimeTest`, `LocalHostRemoteAccessServerTest`, and `OpenAICodexResponsesClientTest` now run successfully.

### March 23, 2026 / 2026 年 3 月 23 日

- 完成真实 Android 设备的构建、安装、重启和 LAN 远控基础验证。Completed real-device build, install, restart, and baseline LAN remote-control validation.
- 完成 Codex 浏览器授权闭环，并确认 `/status` 从 `codexAuthConfigured=false` 变为 `true`。Completed the Codex browser auth loop and confirmed `/status` flipped from `codexAuthConfigured=false` to `true`.
- 在授权完成后定位本机 Host 聊天失败的真实根因：Codex Responses 返回 `400 {"detail":"Unsupported content type"}`。Identified the real post-auth chat failure root cause: Codex Responses returned `400 {"detail":"Unsupported content type"}`.
- 修正 `OpenAICodexResponsesClient` 的请求兼容性：会话 ID 改为 UUID、请求头对齐 Codex、错误信息保留结构化细节，并把请求体收敛成单一 `application/json`。Fixed request compatibility in `OpenAICodexResponsesClient`: session IDs now use UUIDs, request headers align with Codex, error messages keep structured details, and the request body is forced to a single `application/json` content type.
- 新增并通过 `OpenAICodexResponsesClientTest`，把请求形状与错误透传锁成回归测试。Added and passed `OpenAICodexResponsesClientTest` to lock the request shape and structured error surfacing into regression coverage.
- 重新安装最新 APK 后，远控 `/chat/send-wait` 成功返回 `Android local host is working.`，标志着 Android 本机 Host + Codex + LAN 远控 happy path 成立。After reinstalling the latest APK, remote `/chat/send-wait` successfully returned `Android local host is working.`, confirming the Android local-host + Codex + LAN remote-control happy path.
- 运行仓库内建冒烟脚本成功，证明不是只靠手工 `curl` 才能复现。Ran the built-in smoke script successfully, proving the flow is not limited to ad-hoc manual `curl`.
- 新增 `LocalHostCodexAuthController` 和远控 `auth/codex/*` 路由，把 Codex 授权状态与主动 refresh 做成可重复的真机验证面。Added `LocalHostCodexAuthController` and remote `auth/codex/*` routes so Codex auth status and proactive refresh are repeatable on-device validation surfaces.
- 在真机上验证主动 refresh 成功，且 refresh 后的聊天链路仍然可用。Validated proactive refresh on the phone and confirmed chat still works afterward.
- 新增 `local-host-permission-smoke.sh`，把权限缺失失败场景变成可直接复跑的脚本。Added `local-host-permission-smoke.sh` to turn permission-missing failure cases into a directly repeatable script.
- 记录了这台 Android 15 设备的一个行为差异：shell 缺少 runtime revoke / appops 修改能力，因此脚本在权限已被拒绝时会直接验证失败路径，只在权限已授予时才尝试临时撤回。Captured a device-specific behavior on this Android 15 phone: the shell lacks runtime revoke / appops mutation privileges, so the script validates already-denied cases directly and only attempts temporary revocation when a permission starts granted.
- 当前推荐的已验证基线是提交 `9b492a1fca` 以及后续文档提交。The recommended verified baseline is commit `9b492a1fca` plus the follow-up documentation commit.

### March 24, 2026 / 2026 年 3 月 24 日

- 新增 `Dedicated host deployment` 开关，把闲置手机部署模式直接暴露在 `Connect -> Local Host`。Added a `Dedicated host deployment` switch so the idle-phone deployment mode is directly exposed in `Connect -> Local Host`.
- 新增 `apps/android/app/src/main/java/ai/openclaw/app/LocalHostDedicatedDeploymentManager.kt`，把“是否应保活”收敛成单点判断。Added `apps/android/app/src/main/java/ai/openclaw/app/LocalHostDedicatedDeploymentManager.kt` to centralize the "should this phone keep the host alive" decision.
- 新增 `apps/android/app/src/main/java/ai/openclaw/app/LocalHostBootReceiver.kt` 和 manifest 接线，使 `BOOT_COMPLETED`、`MY_PACKAGE_REPLACED`、`USER_UNLOCKED` 可以恢复本机 Host 前台服务。Added `apps/android/app/src/main/java/ai/openclaw/app/LocalHostBootReceiver.kt` plus manifest wiring so `BOOT_COMPLETED`, `MY_PACKAGE_REPLACED`, and `USER_UNLOCKED` can restore the local-host foreground service.
- `apps/android/app/src/main/java/ai/openclaw/app/NodeForegroundService.kt` 现在会在 dedicated 模式下观察 `Local Host` 掉线，并延迟触发一次自愈重连。`apps/android/app/src/main/java/ai/openclaw/app/NodeForegroundService.kt` now watches for Local Host disconnects in dedicated mode and triggers a delayed self-heal reconnect.
- `apps/android/app/src/main/java/ai/openclaw/app/NodeRuntime.kt` 的前台状态默认值改为 `false`，避免服务侧拉起 runtime 时错误放开前台限定命令。The default foreground state in `apps/android/app/src/main/java/ai/openclaw/app/NodeRuntime.kt` is now `false` so service-started runtimes do not accidentally unlock foreground-only commands.
- 新增 `apps/android/app/src/test/java/ai/openclaw/app/LocalHostBootReceiverTest.kt`，并补充 `apps/android/app/src/test/java/ai/openclaw/app/SecurePrefsTest.kt`，覆盖 dedicated 开关持久化、保活条件判断和开机拉起。Added `apps/android/app/src/test/java/ai/openclaw/app/LocalHostBootReceiverTest.kt` and expanded `apps/android/app/src/test/java/ai/openclaw/app/SecurePrefsTest.kt` to cover dedicated-mode persistence, keepalive decision logic, and boot-time startup.
- 在当前机器上运行定向 Android 单测：`SecurePrefsTest`、`LocalHostBootReceiverTest`、`NodeForegroundServiceTest`、`LocalHostWorkspaceToolingTest`、`LocalHostNodesToolingTest`、`LocalHostRuntimeTest`、`LocalHostRemoteAccessServerTest`、`OpenAICodexResponsesClientTest`，结果 `BUILD SUCCESSFUL`。Ran the targeted Android unit tests on the current machine: `SecurePrefsTest`, `LocalHostBootReceiverTest`, `NodeForegroundServiceTest`, `LocalHostWorkspaceToolingTest`, `LocalHostNodesToolingTest`, `LocalHostRuntimeTest`, `LocalHostRemoteAccessServerTest`, and `OpenAICodexResponsesClientTest`, and the result was `BUILD SUCCESSFUL`.
- 新增 `apps/android/app/src/main/java/ai/openclaw/app/DedicatedHostSupport.kt`，把电池优化豁免检查与跳转逻辑集中管理，并把该状态接进 `apps/android/app/src/main/java/ai/openclaw/app/ui/ConnectTabScreen.kt` 和 `apps/android/app/src/main/java/ai/openclaw/app/node/DeviceHandler.kt`。Added `apps/android/app/src/main/java/ai/openclaw/app/DedicatedHostSupport.kt` to centralize battery-optimization exemption checks plus intents, and wired that state into `apps/android/app/src/main/java/ai/openclaw/app/ui/ConnectTabScreen.kt` and `apps/android/app/src/main/java/ai/openclaw/app/node/DeviceHandler.kt`.
- 再次运行定向 Android 单测：`DeviceHandlerTest`、`SecurePrefsTest`、`LocalHostBootReceiverTest`、`NodeForegroundServiceTest`、`LocalHostWorkspaceToolingTest`、`LocalHostNodesToolingTest`、`LocalHostRuntimeTest`、`LocalHostRemoteAccessServerTest`、`OpenAICodexResponsesClientTest`，结果 `BUILD SUCCESSFUL`。Reran the targeted Android unit tests: `DeviceHandlerTest`, `SecurePrefsTest`, `LocalHostBootReceiverTest`, `NodeForegroundServiceTest`, `LocalHostWorkspaceToolingTest`, `LocalHostNodesToolingTest`, `LocalHostRuntimeTest`, `LocalHostRemoteAccessServerTest`, and `OpenAICodexResponsesClientTest`, and the result was `BUILD SUCCESSFUL`.
- dedicated keepalive 现在补上了服务恢复闹钟与远控 readiness 快照：`apps/android/app/src/main/java/ai/openclaw/app/NodeForegroundService.kt` 在 task removed / destroy 时会走 `apps/android/app/src/main/java/ai/openclaw/app/DedicatedHostSupport.kt` 的恢复调度，`apps/android/app/src/main/java/ai/openclaw/app/host/LocalHostRuntime.kt` 则开始把 deployment 状态塞进远控 `/status`。Dedicated keepalive now includes a service-recovery alarm plus a remote readiness snapshot: `apps/android/app/src/main/java/ai/openclaw/app/NodeForegroundService.kt` now routes task-removed / destroy events through the recovery scheduler in `apps/android/app/src/main/java/ai/openclaw/app/DedicatedHostSupport.kt`, while `apps/android/app/src/main/java/ai/openclaw/app/host/LocalHostRuntime.kt` now feeds deployment status into remote `/status`.
- dedicated keepalive 进一步补上了低频 watchdog：`apps/android/app/src/main/java/ai/openclaw/app/NodeForegroundService.kt` 在启动时会重新武装 watchdog，而 `apps/android/app/src/main/java/ai/openclaw/app/DedicatedHostSupport.kt` 会把 `watchdogIntervalMs` 和 `watchdogEnabled` 暴露到 deployment readiness。Dedicated keepalive now also has a low-frequency watchdog: `apps/android/app/src/main/java/ai/openclaw/app/NodeForegroundService.kt` rearms it on service starts, and `apps/android/app/src/main/java/ai/openclaw/app/DedicatedHostSupport.kt` exposes `watchdogIntervalMs` plus `watchdogEnabled` in deployment readiness.
- 重新通过真机 UI 触发电池优化豁免，并确认 `adb shell dumpsys deviceidle whitelist` 出现 `user,ai.openclaw.app,...`，远端 `/status.host.deployment.batteryOptimizationIgnored` 也同步变成 `true`。Triggered the battery-optimization exemption again through the real-device UI and confirmed `adb shell dumpsys deviceidle whitelist` now contains `user,ai.openclaw.app,...`, while remote `/status.host.deployment.batteryOptimizationIgnored` also flips to `true`.
- 在同一台 OPPO / ColorOS V15 真机上重复 `Recents` 划卡测试，系统日志明确记录 `Force stopping ai.openclaw.app ... o-stop(40)`；随后 `stopped=true`、前台服务消失、dedicated 闹钟也被清空。Repeated the Recents swipe-away test on the same OPPO / ColorOS V15 device, and system logs explicitly recorded `Force stopping ai.openclaw.app ... o-stop(40)`; afterward the package was `stopped=true`, the foreground service was gone, and the dedicated alarms were cleared too.
- 因此把 OEM 背景策略风险正式接入 `DedicatedHostSupport`、`DeviceHandler` 和 `ConnectTabScreen`：远端 `device.status.backgroundExecution` 及 `/status.host.deployment` 会回传 `recentsSwipeForceStopRisk` / `taskLockRecommended`，本地 UI 也会提示 dedicated 部署不要划掉卡片。As a result, the OEM background-policy risk is now wired into `DedicatedHostSupport`, `DeviceHandler`, and `ConnectTabScreen`: remote `device.status.backgroundExecution` and `/status.host.deployment` now return `recentsSwipeForceStopRisk` / `taskLockRecommended`, and the local UI now warns dedicated deployments not to swipe the card away.

## Evidence Checklist / 证据清单

Use this section as an operator log when validating the feature on a phone. / 在手机上验证该功能时，可将本节作为操作记录清单。

### Build Evidence / 构建证据

- [x] `./gradlew :app:assembleDebug`
- [x] `./gradlew :app:testDebugUnitTest` (targeted local-host suite / 定向本机 Host 测试集)
- [ ] `./gradlew :app:installDebug` (`adb install` succeeded, but Gradle install hit ddmlib `InstallException: -99` on this device / `adb install` 成功，但 Gradle 安装在该设备上触发 ddmlib `InstallException: -99`)

### Device Evidence / 设备证据

- [x] App 以 `Local Host` 模式成功启动。App starts in `Local Host` mode.
- [x] Codex 登录成功。Codex sign-in succeeds.
- [x] 本地聊天返回 GPT 回复。Local chat returns a GPT response.
- [x] `/status` 返回可用性快照，且当前是 `codexAuthConfigured=true`。`/status` returns a readiness snapshot, currently with `codexAuthConfigured=true`.
- [x] 远端客户端成功完成 `/chat/send-wait` 并收到模型最终回复。A remote client completes `/chat/send-wait` and receives the model's final reply.
- [x] 至少一个只读 `/invoke` 命令成功。One read-only `/invoke` command succeeds.
- [x] 至少一个分层命令仅在对应开关开启时成功。One gated command succeeds only when its tier is enabled.
- [x] 第一条 gated UI 写动作已在真机成功，包括 `ui.tap`、`ui.home`、`ui.back`。The first gated UI write actions now succeed on-device, including `ui.tap`, `ui.home`, and `ui.back`.
- [x] `auth/codex/status` 与 `auth/codex/refresh` 在真机上成功工作。`auth/codex/status` and `auth/codex/refresh` work successfully on a real phone.
- [x] 仓库里已有桌面侧 `pnpm android:local-host:codex-sync`，可在可信连接下复用桌面 Codex OAuth 给手机补授权。The repo now includes `pnpm android:local-host:codex-sync` so a trusted desktop connection can refill phone auth from desktop Codex OAuth.
- [x] dedicated 部署电池优化豁免在真机上成功生效，`/status.host.deployment.batteryOptimizationIgnored=true`。The dedicated-deployment battery exemption now succeeds on-device, with `/status.host.deployment.batteryOptimizationIgnored=true`.

### Failure Evidence / 失败证据

- [x] 非法 bearer token 返回 `401`。Invalid bearer token returns `401`.
- [x] 写操作层关闭时会阻止写命令。Disabled write tier blocks write commands.
- [x] Codex 缺失或过期时会给出清晰失败提示。Expired or missing Codex auth fails clearly.
- [x] 权限依赖命令在权限缺失时会清晰失败。Permission-dependent commands fail clearly when permission is missing.
- [x] 当前 OPPO / ColorOS V15 真机上，`Recents` 划掉卡片会把 dedicated host 直接 `force stop` 并清空闹钟；UI 和 readiness 已开始显式提示该风险。On the current OPPO / ColorOS V15 phone, swiping the card away from Recents directly `force stop`s the dedicated host and clears alarms; the UI and readiness surfaces now warn about that risk explicitly.
- [x] 重新安装 APK 后 accessibility grant 会被系统清空；重新开启无障碍服务后 UI 自动化恢复正常。After reinstalling the APK, the system clears the accessibility grant; once the accessibility service is re-enabled, UI automation recovers normally.

## Recommended Next Slice / 推荐下一步

The MVP close-out gate still passes, and the first bounded phone-control loop now passes on-device too. / 当前 MVP 收尾门槛仍然成立，而且第一条有边界的手机操控闭环现在也已经在真机通过。

The active next slice is no longer “land the first loop.” / 当前激活的下一段工作已经不再是“做出第一条闭环”。

It is now “make this loop repeatable, then expand it from in-app navigation into cross-app tasks.” / 现在最值得推进的是“把这条闭环做成可重复验证，再从 app 内导航扩到跨 app 任务”。

This is now partly landed as a real script too: `apps/android/scripts/local-host-ui-smoke.sh` plus `pnpm android:local-host:ui` turn the current bounded `ui.launchApp -> ui.tap(Chat) -> ui.waitForText(chat-ready) -> ui.tap(editor) -> ui.inputText(write draft) -> ui.inputText(clear draft) -> ui.state` loop into a reusable validation path. / 这件事现在也已经部分落成真实脚本：`apps/android/scripts/local-host-ui-smoke.sh` 和 `pnpm android:local-host:ui` 已把当前有边界的 `ui.launchApp -> ui.tap(Chat) -> ui.waitForText(聊天就绪) -> ui.tap(输入框) -> ui.inputText(写入草稿) -> ui.inputText(清空草稿) -> ui.state` 闭环变成可复用验证路径。

Priority order / 优先顺序:

1. 把新的 `pnpm android:local-host:ui` 当作每次开工的健康基线，先确认默认 in-app smoke 仍可稳定复跑，再继续跨 app 工作。Use the new `pnpm android:local-host:ui` as the start-of-session health baseline so the default in-app smoke is reconfirmed before moving on to cross-app work.
2. 既然新的 `pnpm android:local-host:ui:cross-app:sweep` 已经确认当前设置下 30 秒内都属于 `foregrounded_host_reachable`，而 `pnpm android:local-host:ui:cross-app:next` 与第一条 `ui.swipe` 也都已有真机证据，下一步应优先把这些现有路径维持成稳定基线，再决定是否值得把 swipe 也收成 repo-side follow-up harness，而不是继续把精力放在短到中等长度的纯 reachability 探针上。Now that `pnpm android:local-host:ui:cross-app:sweep` has confirmed `foregrounded_host_reachable` all the way through 30 seconds on the current setup, and both `pnpm android:local-host:ui:cross-app:next` plus the first `ui.swipe` already have on-device evidence, the next step should keep those existing paths stable as a baseline and then decide whether swipe is worth turning into a repo-side follow-up harness instead of spending more time on short-to-medium pure reachability probes.
3. 继续强化 selector 模型，优先资源 ID、包名作用域和更稳定的节点选择顺序，并把 `ui.swipe` 继续限制在 evidence-driven 的坐标手势边界内；只有当更多目标流程都需要滚动时，再考虑把它抽成通用 preset。Keep strengthening the selector model, prioritizing resource IDs, package-name scoping, and more stable node selection order, while keeping `ui.swipe` constrained to an evidence-driven coordinate-gesture boundary; only if more target flows truly need scrolling should it be lifted into a general preset.
4. 按 `pnpm android:local-host:dedicated:readiness` 的结论推进闲置手机专机化：优先准备账号清理 / reset 和 DPC 安装，再决定是否真的要进入 root/systemize 线路。Use the `pnpm android:local-host:dedicated:readiness` result to drive idle-phone dedicated deployment: prioritize account cleanup or reset plus DPC installation before deciding whether the root/systemize lane is actually necessary.

## Tomorrow Tasks / 明天任务

Use this as the shortest checklist for the next working session. / 把这一节当作明天开工时的最短任务清单。

1. 先跑一次 `/status` 和 `ui.state`，确认手机仍在 `Local Host`，并检查 accessibility grant 是否还在。Run `/status` and `ui.state` first to confirm the phone is still in `Local Host` and check whether the accessibility grant is still present.
   Output / 产出:
   - 记录当前 `uiAutomationAvailable` / `uiAutomation` 快照。
   - 若刚重装过 APK，优先确认并恢复无障碍服务。
2. 把新的 `pnpm android:local-host:ui` 当作基线回归，确认默认 in-app smoke 路径仍然可以直接复跑。Use the new `pnpm android:local-host:ui` as the baseline regression check and confirm the default in-app smoke path still replays cleanly.
   Output / 产出:
   - 让第一条真机 phone-control 证据不再只依赖手工步骤。
   - 产出一份固定 artifact 目录下的状态、能力和 UI 自动化结果。
3. 既然 reachability sweep 在 30 秒内没有掉线，而 `ui.swipe` 也已有第一条设置页首页滚动正证据，下一步先把 `ui.launchApp -> follow-up action -> ui.inputText` 维持成真机闭环，再判断是否值得把 swipe 也接进现有 follow-up harness。Now that the reachability sweep stayed healthy through 30 seconds and `ui.swipe` already has its first positive Settings-homepage scroll proof, the next step is to keep `ui.launchApp -> follow-up action -> ui.inputText` as a real-device loop and then decide whether swipe is worth wiring into the existing follow-up harness.
   Output / 产出:
   - 至少有一个能启动目标 app、等待元素、再继续动作的真机闭环雏形。
4. dedicated 线先用 `pnpm android:local-host:dedicated:readiness` 和 `pnpm android:local-host:dedicated:testdpc-install` 重新确认当前手机还卡在哪一步，再决定是否进入 `TestDPC` 安装 / QR provisioning。On the dedicated-device lane, use `pnpm android:local-host:dedicated:readiness` plus `pnpm android:local-host:dedicated:testdpc-install` first to reconfirm exactly where the phone is still blocked before deciding whether to enter TestDPC install / QR provisioning.
   Output / 产出:
   - 明确记录当前是 `TestDPC not installed`、还是已经进入 `Device Owner` / post-provision 阶段。
   - 不在状态还没对齐时直接执行 `--apply` 类命令。
5. 保持当前 `Go / No go` 结论稳定，并继续把授权回跳问题和 true expired-auth 记成非 blocker。Keep the current `Go / No go` verdict stable, and continue treating the auth-return issue plus true expired-auth as non-blockers.
   Output / 产出:
   - 三份文档继续对“当前 MVP 已 `Go`，第一条 phone-control 闭环已成立”给出一致结论。
   - 不要把旧的 streaming 或 usage-limit 记录重新当成实现回归。

Stop point / 收工标准:

- `apps/android/local-host-progress.md`、`apps/android/local-host-self-check.md`、`apps/android/local-host-handoff.md` 三份文档对“当前 MVP 收尾已通过”给出一致结论。
- 不再因为旧的 usage-limit 记录或 streaming 超时记录把问题回滚成实现 blocker。

## Resume Plan / 接续推进计划

Use this section to resume work in a fresh session. / 新会话里继续推进时，优先看这一节。

### Immediate Goal / 当前直接目标

- 把已成立的第一条真机 observe / wait / act 闭环变成可重复验证面，并继续向跨 app 任务扩展。Turn the first working real-device observe / wait / act loop into a repeatable validation surface, then keep expanding it toward cross-app tasks.

### Priority Order / 优先顺序

1. 先保持收尾结论稳定。Keep the close-out verdict stable first.
   Exit criteria / 退出标准:
   - 三份文档都明确写出 streaming 真机直证已经完成。
   - `Go / No go` 不再因为过期 token 的可选加固或授权回跳问题而摇摆。
2. 先重新确认真机上的 UI 自动化基线。Reconfirm the on-device UI-automation baseline.
   Exit criteria / 退出标准:
   - `uiAutomationAvailable` 和 `ui.state` 都能在当前手机上给出清晰结果。
   - 明确记录重装 APK 后 accessibility grant 可能丢失这一行为。
3. 把第一条 bounded control loop 做成可复跑的验证面，并继续补下一批跨 app 动作。Turn the first bounded control loop into a repeatable verification surface and continue with the next batch of cross-app actions.
   Exit criteria / 退出标准:
   - `wait_for_text` 能继续支撑可重复的等待场景。
   - `tap`、`back`、`home` 的真机成功证据已沉淀到文档或脚本。
   - `ui.launchApp + ui.inputText` 保持可重复的真机证据；`ui.swipe` 的第一条真机证据已经建立，接下来只在目标流程确实需要时再决定是否把它产品化成 repo-side harness，并继续记录 OPPO 后台冻结边界。
4. 把 dedicated 专机路线收敛到 `Device Owner` / `TestDPC` 工具链，而不是继续停留在概念讨论。Converge the dedicated-device lane on the `Device Owner` / `TestDPC` toolchain instead of leaving it as a conceptual discussion.
   Exit criteria / 退出标准:
   - `readiness -> testdpc-install -> testdpc-qr -> post-provision -> testdpc-kiosk` 这条链路在文档里顺序清楚。
   - 当前手机到底停在 `TestDPC not installed`、`not Device Owner`，还是已经进入 post-provision 阶段，有明确记录。

### Recommended First Steps For The Next Session / 下一会话建议开局

- 先读 `apps/android/local-host-handoff.md`，确认当前状态、命令和下一步 UI 自动化切入点。Read `apps/android/local-host-handoff.md` first to confirm the current state, commands, and the next UI-automation entry point.
- 如果只是想拿到当前真实诊断，不要再手工串 token / smoke / network probe；直接跑 `pnpm android:local-host:doctor`。If you only need the current ground-truth diagnosis now, do not hand-chain token / smoke / network probe anymore; run `pnpm android:local-host:doctor` directly.
- 先跑一次 `/status` 和 `ui.state`，确认设备仍在 `Local Host` 且无障碍服务状态可见；若刚重装过 APK，先恢复 accessibility grant。Run `/status` and `ui.state` to confirm the device is still in `Local Host` and the accessibility-service state is visible; if the APK was just reinstalled, restore the accessibility grant first.
- 把 `pnpm android:local-host:smoke` 当成远端真值基线来复跑：它现在会在 `chat state=error`、chat 超时或 `invoke.ok=false` 时直接失败，并把 `summary.json` 与错误分类落盘。Use `pnpm android:local-host:smoke` as the ground-truth remote baseline: it now fails directly on `chat state=error`, chat timeout, or `invoke.ok=false`, and persists both `summary.json` and the classified failure fields.
- 如果 smoke 再次落到 `openai_connect_timeout`，下一步直接跑 `pnpm android:local-host:openai-network`，先确认是 `chatgpt.com`、`auth.openai.com` 还是特定协议族的出站路径有问题。If smoke lands at `openai_connect_timeout` again, run `pnpm android:local-host:openai-network` next so the team can quickly tell whether the broken outbound path is `chatgpt.com`, `auth.openai.com`, or only one IP family.
- 在扩更多命令前，先把当前 `ui.state` / `ui.waitForText` / `ui.tap` / `ui.home` / `ui.back` 组合成可重复的 smoke 路径。Before expanding any broader command surface, turn the current `ui.state` / `ui.waitForText` / `ui.tap` / `ui.home` / `ui.back` set into a repeatable smoke path first.
- 当前仓库已经有第一版 UI smoke 脚本：`apps/android/scripts/local-host-ui-smoke.sh`，默认先停留在 OpenClaw Chat 页完成 `launchApp + tap + waitForText + inputText(写入/清空草稿) + state`，并把跨 app probe 继续留作可选项。The repo now has a first-pass UI smoke script at `apps/android/scripts/local-host-ui-smoke.sh`; by default it stays inside the OpenClaw Chat tab to complete `launchApp + tap + waitForText + inputText(write/clear draft) + state`, while keeping the cross-app probe optional.
- 如果下一轮要继续做“页面可切中英文”的收尾，优先补剩余的次级页面、深层 runtime / auth 错误文本、较少见的 gateway/control-ui 边缘态和零散状态文案；前台通知标题/正文/断开按钮与通知 channel 文案、通知操作失败、`system.notify` 常见失败、相机采集常见失败、系统权限弹窗文案，以及设置里关闭 capability 时的常见 `_DISABLED` 提示现在也已经跟随 `Settings -> Language` 切换，不需要回头重做。If the next round continues the "switchable English / Chinese UI" work, prioritize the remaining secondary pages, deeper runtime/auth error text, rarer gateway/control-ui edge states, and scattered status copy; the foreground notification title/body/disconnect action and notification-channel copy, plus common notification-action failures, `system.notify` failures, camera-capture failures, system permission-dialog copy, and common capability-disabled `_DISABLED` warnings now also follow `Settings -> Language`, so they do not need to be revisited.
- 当前仓库也已经有 dedicated 工具链入口：先跑 `pnpm android:local-host:dedicated:readiness` 和 `pnpm android:local-host:dedicated:testdpc-install` 看状态，再按需要继续到 `pnpm android:local-host:dedicated:testdpc-qr`、`pnpm android:local-host:dedicated:post-provision`、`pnpm android:local-host:dedicated:testdpc-kiosk`。The repo now also has a dedicated-device toolchain entrypoint: run `pnpm android:local-host:dedicated:readiness` plus `pnpm android:local-host:dedicated:testdpc-install` first to inspect state, then continue to `pnpm android:local-host:dedicated:testdpc-qr`, `pnpm android:local-host:dedicated:post-provision`, and `pnpm android:local-host:dedicated:testdpc-kiosk` as needed.
