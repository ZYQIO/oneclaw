# Offline Task Brief

Use this packaged template when the Android embedded runtime pod needs a bounded, offline-only starting point.

- Keep the workflow deterministic.
- Do not require browser access.
- Do not execute arbitrary shell commands.
